# Pi RP 基础设施升级需求台账

更新时间：2026-09-13

## 用途

本文件只登记已经由用户确认、适合在 Pi RP 顶层框架解决的升级需求，包括功能模块系统、统一数据协议、Workflow、Agent 工具、上下文、产物路由和公共运行时能力。

具体模块自身的数据字段、业务规则、提示词和领域处理不记在这里。讨论中发现公共能力可能不足时，也不能立即把它登记为顶层升级项；必须先比较模块内实现与顶层升级，再向用户提出明确方案。

登记不代表已经授权实施。实际升级仍需等待合适时机，并再次明确修改范围、兼容影响、迁移需求和验证方案。

## 核心原则

既不轻易升级顶层框架，也不为了维持现有顶层框架而让具体模块采用曲折、重复或难以维护的实现。

优先由具体模块解决的情况：

- 行为只属于该模块的领域语义；
- 现有公共接口可以直接、清晰且安全地支持；
- 模块内实现不需要绕过权限、预算、事务或会话边界；
- 不会在多个模块中重复建设同一套基础能力；
- 不需要把模块业务规则硬编码进公共运行时。

适合提出顶层升级方案的情况：

- 缺少一个明显可复用的公共原语或受控扩展点；
- 模块内实现必须修改或硬编码公共运行时才能工作；
- 为兼容现状需要多层中转、重复序列化、反复模型调用或大量胶水代码；
- 模块内绕行会削弱权限、预算、一致性、事务、隔离或验证能力；
- 多个功能模块可以共享同一能力，且公共实现能显著减少重复复杂度；
- 现有协议无法准确表达已经确认的语义，而不是仅仅写起来不方便。

若两种方式都能合理实现，不因“做成公共能力看起来更整齐”而升级顶层；也不因“现有顶层已经如此”而默认接受明显不合理的模块实现。

## 登记前流程

发现疑似公共缺口时，按以下顺序处理：

1. 先设计一条不修改顶层框架的直接模块实现路径。
2. 判断该路径是否清晰、安全、可测试、可维护，是否产生重复或不必要的模型与 token 开销。
3. 如果顶层升级可能明显更合适，先在对话中向用户说明：
   - 当前受限的是顶层哪个系统；
   - 建议进行什么具体升级；
   - 该升级能删除或简化当前模块的哪些功能、胶水代码或工作流步骤；
   - 不升级时模块可以怎样实现，以及实际代价；
   - 顶层升级对其他模块的复用价值；
   - 兼容、迁移、安全和验证风险；
   - 当前模块是否会因此阻塞，还是可以继续设计。
4. 等待用户明确选择继续模块内实现，或确认将该问题登记为顶层升级需求。
5. 只有用户确认后，才在本文件新增正式条目。
6. 已登记条目等待用户选择合适时机实施；不得因已经登记而自动修改公共系统。

## 提案格式

在登记前，优先使用以下简要对照向用户说明：

```markdown
### 发现的公共能力问题

涉及系统: <功能模块系统 / 统一数据协议 / Workflow / 公共运行时等>

模块内实现:
- <实现方式>
- <复杂度、限制和风险>

顶层升级:
- <准确的升级内容>
- <能从具体模块中删除或简化的内容>
- <对其他模块的复用价值>

影响:
- 兼容: <无 / 具体影响>
- 迁移: <无 / 具体需求>
- 安全与权限: <边界>
- 验证: <需要补充的测试>

建议: <保留在模块内 / 建议登记顶层升级>
```

提案应具体到可以判断取舍，不能只说“顶层不支持”“最好升级协议”。

## 状态说明

- **已确认待排期**：用户已确认适合登记为顶层升级，但尚未选择实施时机。
- **已规划**：升级范围、方案、兼容边界和验证计划已经确认。
- **实施中**：用户已经授权当前升级，修改尚未完成。
- **已实施**：公共实现、文档和验证已经落地。
- **取消**：用户取消升级，或后续确认应由具体模块负责。

本文件不设置“候选”“观察”或“待用户确认”条目。未经确认的分析只保留在当时的设计讨论中，避免把尚未完成取舍的问题误当成公共升级计划。

## 正式条目格式

```markdown
## INFRA-XXX：<名称>

状态：**已实施**

确认日期：YYYY-MM-DD

来源模块：<模块或设计任务>

涉及系统：<准确的公共目标>

确认的升级内容：

- <内容>

对具体模块的简化：

- <将删除、替代或简化的模块实现>

不升级时的模块路径及代价：

- <可行路径与实际代价>

兼容与迁移：

- <影响>

安全边界：

- <权限、数据、执行和隔离边界>

验证要求：

- <测试>

实施授权：尚未授权；等待用户选择合适时机。
```

## 正式升级条目

## INFRA-001：回合后台工作流阻止下一轮用户输入

状态：**已实施**

确认日期：2026-09-05

规划确认日期：2026-09-06

实施日期：2026-09-06

来源模块：叙事记忆与记录模块设计

涉及系统：Workflow v2 契约、工作流调度运行时、回合提交与前端运行状态

确认的升级内容：

- 为 `turn-background` 工作流增加由作者静态声明的回合阻塞能力，使指定后台工作流启动后，可以阻止当前会话接受下一条用户消息，直到本次工作流按照声明的终止策略完成。
- 将阻塞状态与触发该实例的会话、剧情分支、消息和回合绑定；支持手动、工作流完成后和指定节点完成后三种现有后台触发方式。
- 由调度器聚合同一回合中的多个阻塞实例，并与 `turn-finalize` 协调；所有必需阻塞实例解除前，不推进或接受下一轮。
- 明确成功、失败、等待模型选择、取消和显式跳过时的释放规则。失败不得静默放行，也不得永久锁死会话；前端需要向用户提供重试、更换模型、取消或明确跳过等处置入口。
- 在运行时状态与前端中显示具体阻塞实例和原因；下一条用户输入到达时由公共运行时统一拒绝或暂缓，而不是交给功能模块自行拦截。
- 补充 Workflow v2 的机器字段、校验规则与运行时恢复语义。现有资料虽提及 `blocking behavior`，标准工作流描述也暗示后台功能完成后再提交回合，但当前公开工作流结构和校验器没有对应的可声明字段。

对具体模块的简化：

- 叙事记忆模块只需把独立归档工作流声明为阻塞型回合后台工作流，不必自行维护会话锁、轮询后台实例、拦截用户输入或处理跨工作流解锁。
- 冷却未到期或没有可归档内容时，归档工作流可以由确定性门控立即成功，不形成持续阻塞；真正运行两级归档节点时，由公共调度器保证下一轮不会提前开始。
- 变量结算、幕后事件、资源结算等其他必须在下一轮前完成的后台工作流可以复用同一能力。

不升级时的模块路径及代价：

- 记忆模块需要维护模块专属的会话级锁和后台实例状态，并在正文入口处增加查询、轮询、超时、失败处置和解锁胶水代码。
- 该路径会让领域模块侵入公共输入与调度边界，容易在取消、重试、运行时重启、多个后台任务并发和分支回退时产生死锁或提前放行；其他模块还会重复建设同类机制。

兼容与迁移：

- 新字段应为可选，未声明的现有后台工作流继续保持非阻塞语义。
- 现有工作流和会话数据原则上不需要迁移；只有希望启用该能力的卡或工作流需要显式增加声明。
- 实施时需要核对现有 `turn-finalize` 行为，避免同一回合同时存在旧式内嵌后台节点和新式独立阻塞工作流时重复等待或无法完成。

安全边界：

- 只有卡或工作流作者的静态配置可以授予阻塞能力，普通 Agent 输出和模块数据不能动态获得或扩大该权限。
- 阻塞范围仅限触发实例所在的会话与回合，不得影响其他会话。
- 用户始终保留可见的恢复与显式跳过入口；跳过行为必须留下运行状态或回执，不能伪装成成功归档。
- 阻塞型工作流不得等待未来用户输入作为自身完成条件，校验器应拒绝明显的触发或依赖死循环。

验证要求：

- 覆盖手动、工作流完成后和指定节点完成后三种触发方式下的阻塞与释放。
- 覆盖成功、失败、重试、更换模型、取消、显式跳过、运行时重启恢复和剧情分支回退。
- 验证多个阻塞实例的聚合、实例去重、非阻塞工作流兼容、下一条输入拒绝以及会话之间的隔离。
- 验证冷却门控立即结束时不会造成不必要阻塞，并验证阻塞工作流不能通过循环依赖永久占用回合。

实施结果：

- Workflow v2 节点现以显式 `blockNextTurn: true` 取得阻塞能力；未声明的既有后台工作流保持非阻塞，其他工作流种类声明该能力会被校验器拒绝。
- 工作流引擎基于实例启动时保存的规范化定义聚合阻塞状态，并向 Web 状态与提交接口提供同一判断源；运行中修改工作流文件不会改变既有实例的锁定语义。
- 待执行、执行中、等待重试和等待模型选择的阻塞节点会保持会话锁；完成、条件或冷却跳过、失败、取消会释放对应节点。显式跳过会把实例记录为 `skipped`，不会伪装成成功，也不会触发成功型 `after-workflow` 链。
- 前端会显示阻塞工作流名称、禁用输入，并在工作流实例中标识“正在阻止下一轮”；现有重试、换模型、取消入口之外，增加“跳过并放行”。后端提交接口独立重复校验，不能绕过前端直接提交。
- 未完成实例恢复后继续采用相同规则；运行时重启打断的节点转为等待模型选择并保持阻塞。叙事记忆的来源捕获与两级归档节点已显式启用该能力。

验证结果：Workflow 定义、引擎阻塞聚合、失败等待、取消、显式跳过、运行时恢复和记忆模块工作流测试均已通过。

## INFRA-002：转卡前置全局模块选择与方案承载变化披露

状态：**已实施**

确认日期：2026-09-05

来源模块：叙事记忆与记录模块设计

涉及系统：`st-card-to-pi-rp` 转卡技能、全局模块发现与导入规划、转换提案确认流程

确认的升级内容：

- 转卡技能启动后，在制定转换方案之前优先读取可用全局模块的必要目录信息，并询问用户本次希望引入哪些全局模块；大型全局模块必须作为转换方案的前置设计条件，而不能等完整方案形成后再作为附加项询问。
- 用户已经在初始请求中明确选择、不选择或授权 Agent 代选全局模块时，不重复询问；没有可用全局模块时明确说明后继续，不提出空问题。
- 模块选择只确定方案范围，不跳过原有的转换提案与整体确认门槛。转卡 Agent 需要在选定模块后联合分析原卡内容、数据所有权、上下文、工作流、开局和前端影响，再提交完整方案。
- 当方案引入叙事记忆模块时，必须向用户或转卡者明确列出哪些原卡信息将从固定提示词、按需静态资料或其他原有承载方式，转而由记忆模块的初始实体、事件、关系、认知、群体、信息控制或配套工作流实现。
- 记忆模块方案同时说明继续保留为固定提示词的内容、继续由其他模块拥有的权威状态、仅保留的短提示锚点、开局差异、其他模块适配、归档来源保存、归档工作流获取方式及必要权限。不得只用“已集成记忆模块”概括这些变化。
- 对其他会显著改变转换结构的大型全局模块采用同类透明原则：方案必须披露其接管、替代、拆分或重新路由的原卡语义，避免用户在确认后才发现内容承载方式已经改变。

对具体模块的简化：

- 记忆模块无需在转换方案已经完成后再反向要求重拆固定上下文、实体资料、历史和工作流；转卡 Agent 从一开始就按已选模块设计初始记录、检索、归档和适配。
- 其他大型全局模块也可以在方案阶段取得正确的数据所有权和工作流位置，减少导入后的重复分析、迁移和卡级补丁。
- 用户可以在正式转换前看清哪些原卡内容仍然逐轮固定提供，哪些改为按需检索或结构化维护，从而对 token、表现和忠实度变化作出真实选择。

不升级时的模块路径及代价：

- 继续在完整转换方案末尾才询问全局模块，选中记忆等大型模块后再人工推翻并重写方案中的上下文、数据、工作流和开局设计。
- 或者把大型模块作为表面附加项导入，不充分迁移原卡信息与数据所有权，造成固定提示词和模块记录重复、适配缺失、权限不足或行为与用户预期不一致。

兼容与迁移：

- 该升级只改变未来转卡任务的询问顺序、分析流程和提案内容，不自动修改既有卡、已复制的全局模块或会话数据。
- 已有转换若后来引入记忆模块，仍属于显式卡级修改；需要重新提出承载变化和适配方案，并在用户确认后实施，不因本条目自动迁移。
- 转卡技能现有“先给出有用提案、避免前置连续提问”的规则需要调整，允许且要求一次必要的全局模块前置选择；其余低影响问题仍应留到初步方案后讨论。

安全边界：

- 全局模块选择不等于授权立即写入目标卡；正式转换仍需用户确认完整方案。
- 只读取模块发现和方案制定所需的清单、清单描述及选中模块说明；未选模块不能因此获得目标卡权限或被静默导入。
- 转卡 Agent 不能根据“看起来适合”擅自选择全局模块，除非用户明确授权代选。
- 从原卡转移信息承载方式时仍需保持来源语义、信息控制和 provenance，不得借模块化之名改写或扩展原设定。

验证要求：

- 验证存在多个、单个和零个可用全局模块时的启动流程，以及用户已明确选择、明确不选和授权代选三种路径。
- 验证未完成前置模块选择时不会产出假定模块集合的正式转换方案，选择后不会跳过整体方案确认。
- 对选择记忆模块的案例，检查提案是否逐项披露转入记忆模块、继续固定、继续由其他模块拥有和仅保留锚点的内容，并包含初始数据、开局差异、其他模块适配、延迟来源与归档权限。
- 验证未选择记忆模块时不会擅自把原卡内容迁入该模块；验证既有卡和会话不会因技能升级被自动修改。

实施日期：2026-09-06

实施结果：

- 转卡 skill 现在先完成足以判断相关性的源资料检查与全局模块发现，再用一次紧凑的前置选择确定模块范围；范围未解决前不得形成正式转换方案。
- 用户已经明确选择、明确不选或授权代选时不重复询问；零模块时只说明发现结果。模块预选不授权写入，也不替代后续完整方案确认。
- 正式方案只详细设计已选模块。选择 `narrative-memory` 时，必须单列承载变化矩阵，披露迁入记忆、继续固定、由其他模块拥有、仅保留锚点、各开局初始记录、其他模块来源适配、延迟来源、权限和修订追踪。
- 主要角色性格与行为指导不得随意缩减；迁入记忆的原卡文本继续遵循原文优先、拆分重组和 provenance 规则。对其他模块数据结构的适度调整必须说明风险并另行取得用户同意。

验证结果：新增转卡流程契约测试，验证全局模块预选严格早于正式方案，并验证记忆模块承载变化的必填披露项。

## INFRA-003：消息与工作流产物保留叙事来源层级

状态：**已实施**

确认日期：2026-09-06

来源模块：叙事记忆与记录模块设计

涉及系统：Workflow v2 契约、消息与转录记录、工作流产物路由、上下文组装与延迟来源保存

确认的升级内容：

- 为进入工作流上下文、转录记录和可保留产物的来源增加明确、可持续传递的来源语义，至少区分“由谁产生”和“以什么叙事层级产生”两个彼此独立的维度，避免把 `user`、`agent` 等技术产生者身份直接等同于作者权威。
- 叙事层级至少需要表达 `in-world`、`story`、`authorial`：`in-world` 表示角色在世界内的言行、回忆、证词或认定；`story` 表示正文叙事容器；`authorial` 表示工作流明确指定的作者、编剧、导演或其他设定层。需要角色归属时，允许同时保存可选的稳定角色 ID。
- 用户消息必须依据当前明确的交互身份解释。用户以玩家角色身份发言时属于 `in-world`，不能因为产生者是用户而获得作者权威；只有显式进入或由工作流指定的作者层输入才能标记为 `authorial`。
- 工作流或卡作者应能为节点输入输出静态声明叙事层级。普通 Agent 生成的文本、角色自称、肯定措辞和模型自行输出的标签都不能把来源提升到 `authorial`。
- 来源语义需要随消息保存、编辑、重试、工作流传递、后台触发、延迟归档、会话恢复和分支处理一同保留，供获得相应上下文权限的节点使用；不能只存在于前端临时状态或依靠模块根据文本猜测。
- `story` 只说明正文容器的叙事位置，不把其中每句话都自动确认为客观事实。角色对白、内心回忆、书信、日记和转述等嵌套内容仍由领域节点按语义识别为世界内陈述。
- 具体机器字段、混合内容的表达方式、旧消息的保守默认值和可见视图在正式规划阶段确定；默认策略必须保证未标记来源不能被误当成作者确认。

对具体模块的简化：

- 叙事记忆模块的延迟来源包可以直接保留统一的产生者、叙事层级和角色归属，不必为每一种正文工作流、幕后系统和用户输入方式分别编写身份猜测适配器。
- 二级归档节点可以稳定区分客观叙事、角色认知和作者确认：此前从未出现的过去往事若只由角色讲述，则归入认知；只有明确的作者层确认或已有客观来源才能建立为历史事件。
- 用户以玩家角色身份作出的认定不会被错误提升为事件事实；作者后来确认时，模块可以另建事件并关联原认知，而不需要追溯猜测当时的用户意图。
- 世界状态、任务、关系、声望、幕后事件等其他模块也可以复用同一来源语义，减少各模块重复判断“这是角色说法还是设定事实”的提示词和胶水代码。

不升级时的模块路径及代价：

- 每个需要判断来源权威的模块都必须自行读取工作流节点、消息角色、卡级约定和上下文位置，再为延迟归档额外保存一套模块私有来源标签。
- 该路径可以工作，但不同模块容易采用不一致的权威规则；消息编辑、工作流替换、跨节点产物、延迟处理和会话恢复也可能丢失原始身份，迫使 Agent 仅凭措辞猜测事实层级。
- 记忆模块必须增加保守兜底：无法确认来源层级的新增往事一律按角色认知处理。这能避免擅自写成事实，但会增加人工确认和后续修正成本。

兼容与迁移：

- 新来源字段应采用向后兼容的可选扩展；现有工作流、消息和会话不能因为缺少字段而被自动解释成 `authorial`。
- 旧记录缺少可靠来源时应保留为未指定或采用保守的非作者默认，不通过批量猜测补写作者权威。若特定卡需要恢复旧来源语义，应另行制定显式迁移或卡级适配方案。
- 工作流节点增加静态声明后，转换和模块导入流程需要保存该声明；已有卡只有在显式升级对应工作流时才获得更精确的来源层级。
- 消息编辑若改变了交互身份或叙事层级，消息自身的新修订必须保存新的来源语义；但手动编辑属于非正常游玩路径，不因此自动判定其他模块记录失效、自动重跑工作流或触发跨模块级联处理。用户需要通过显式维护操作处理已经产生的不一致。

安全边界：

- `authorial` 只能来自卡或工作流作者的静态配置，或用户在受控界面中明确选择的作者层输入；普通 Agent、模块记录和正文内容不能自行声明或扩大这一权威。
- 来源层级只描述语义权威，不授予数据查询、写入、维护、工作流控制或信息披露权限；实际能力仍由节点的 `moduleAccess`、视图和提交契约控制。
- 角色 ID 只用于表达世界内发言者或归属，必须遵守当前节点可见的身份注册与信息隔离规则，不得借来源元数据暴露隐藏身份。
- `story` 内容中的角色陈述不能因外层容器是正文而自动晋升为客观事件；多个角色重复、相互印证或高度确信也不能替代作者层确认。

验证要求：

- 覆盖用户作为玩家角色、用户显式作为作者、正文 Agent、角色 Agent、导演节点、幕后系统节点和普通模块产物的来源标记与传递。
- 验证角色讲述此前未记录的往事只产生认知候选，作者层后来确认时才能产生历史事件，并验证原认知及其知情范围仍被保留。
- 覆盖消息保存、编辑、重试、后台触发、延迟归档、会话恢复、分支回退和后缀删除，确认来源语义不会丢失、串位或被旧产物错误复用。
- 验证普通 Agent 伪造标签、角色自称“这是事实”、用户玩家角色作出肯定陈述以及多个角色交叉作证时，都不能获得 `authorial` 权威。
- 验证旧消息缺少来源字段时采用保守兼容路径，不会被静默视为作者确认；验证来源字段不会绕过模块访问权限或严格视角过滤。

实施日期：2026-09-06

实施结果：

- 公共运行时新增独立的技术产生者与叙事层级元数据；玩家输入固定为 `in-world`，开局与普通正文固定为 `story`，旧记录保守归为 `unspecified`。
- Workflow v2 节点只允许作者静态声明 `narrativeSource.layer` 与可选 `characterId`；产生者类型和 ID 由运行时推导，Agent 输出无法自行提升为 `authorial`。
- 消息修订、工作流运行、节点输出、上游产物、自动/Agent 消息检索、后台触发、会话恢复及记忆归档上下文均保留并显示来源层级。
- 来源语义与数据权限保持分离；它不改变 `moduleAccess`、view、工具或提交能力。

验证结果：Narrative source、消息 envelope、Workflow 定义/运行、引擎恢复与卡包静态校验测试均已通过。

## INFRA-004：多来源修订追踪与显式数据修复

状态：**已实施**

确认日期：2026-09-06

来源模块：叙事记忆与记录模块设计

涉及系统：统一数据记录绑定、事务来源追踪、消息修订、维护工具与工作流来源保存

确认的升级内容：

- 在现有单一 `binding.messageId` 和回合绑定之外，为持久数据事务保留可选的多来源修订集合，能够记录本次事务实际依据的消息 ID 与对应修订。单一绑定继续表达记录或归档批次归属于哪一条消息、哪一回合；多来源集合只表达输入来源和修订依据。
- 多轮归档可以继续统一关联本批最后一轮，同时把本批实际处理的全部来源消息及修订写入公共来源追踪。来源集合应由运行时根据工作流已声明并实际使用的输入确定，不能由普通 Agent 任意伪造。
- 多来源修订默认只用于审计、差异检测和显式修复，不构成自动依赖失效机制。用户手动编辑已经归档的旧消息时，公共运行时不得自动使大量模块记录失效、级联回退、重跑工作流或阻塞下一轮；用户承担非正常编辑造成的数据不一致，并决定是否维护。
- 运行时可以提供只读检查能力，按消息及修订列出可能受影响的事务、模块和归档批次，但检查本身不改变任何权威数据，也不把记录标成不可用。
- 提供显式、受权限控制的模块级或指定范围修复入口。用户选择修复后，维护工作流才能利用来源追踪定位需要重新读取、回退或重建的范围；一次修复不得默认扩展到所有相关模块。
- 删除、截断、正常分支回退等明确改变剧情后缀的操作继续采用现有绑定和后缀清理语义，恢复最近仍然有效的记录修订。这些正常结构性操作与“原地手动编辑文本”分开处理。
- 记忆模块仍需保存延迟归档所需的当时来源内容；公共多来源追踪负责标识这些内容来自哪些消息修订，不替代模块适配器保存其他模块输出及必要快照。

对具体模块的简化：

- 叙事记忆模块不必单独发明消息修订索引，就能在用户明确要求修复时定位哪些归档批次曾使用被编辑的消息，以及当时依据的具体修订。
- 多轮归档仍可作为一段连续剧情统一分析并绑定到最后一轮，不会因此失去对所有输入消息的来源追踪。
- 变量、任务、关系、声望等其他从正文生成持久数据的模块也可以复用同一只读追踪与显式修复入口，而不承担自动跨模块级联失效的运行成本和风险。
- 正常游玩路径不会因为用户的非标准手动编辑被迫执行昂贵的全量重算；记忆模块只在用户明确选择修复时启动相应维护流程。

不升级时的模块路径及代价：

- 记忆模块需要在私有归档清单中自行保存消息 ID、修订和归档批次映射，并实现模块专属的影响范围查询与手动重建入口。
- 该路径可以满足记忆模块，但其他正文派生模块会重复保存相同映射；各模块对修订、范围和回合的理解可能不一致，用户也难以统一查看一次编辑可能影响哪些数据。
- 若模块不建设私有追踪，只能在用户发现矛盾后进行范围较大的人工重建，无法准确定位受影响归档批次。

兼容与迁移：

- 多来源追踪应为可选扩展；现有记录继续使用单一消息与回合绑定，不因缺少来源集合而改变删除、回退或查询行为。
- 不为旧事务猜测或批量补写来源修订。旧数据只有在后续显式维护或新事务确实读取时，才产生新的、可验证的来源追踪。
- 手动编辑现有消息不会自动迁移或修改任何模块记录。只读检查可以报告“缺少可追踪来源”，但不能据此推定具体记录已经错误。
- 现有后缀删除与修订恢复语义保持不变；实施时需要明确原地编辑、删除截断、分支重建和显式修复四类操作的不同处理路径。

安全边界：

- 来源集合由公共运行时根据工作流输入注册和事务提交上下文填写；普通 Agent只能产出领域变更，不能伪造来源、把未读取的消息登记为依据或借来源追踪扩大数据权限。
- 只读影响检查仅返回调用者有权查看的模块、事务和安全元数据，不暴露隐藏正文、私有工作流产物或严格视角信息。
- 显式修复必须指定目标模块或范围并使用独立维护权限；发现潜在影响不等于获得查询、回退或重建其他模块的权限。
- 手动编辑不得自动触发 `INFRA-001` 的回合阻塞能力；只有用户显式启动并已获授权的修复或归档工作流，才按照自身声明决定是否阻塞。

验证要求：

- 验证多轮归档在保留最后一轮归属绑定的同时，准确记录所有实际输入消息及其修订，并拒绝 Agent伪造来源集合。
- 验证用户原地编辑已归档消息后，其他模块记录仍保持当前有效状态，不自动失效、不级联回退、不自动重跑且不阻止下一轮。
- 验证只读影响检查能定位具有可追踪来源的事务，也能对旧记录明确报告来源不足，而不修改数据或泄露无权查看的内容。
- 验证用户显式选择单个模块、指定归档批次或限定回合范围进行修复时，不会意外重建其他模块；验证取消和失败不会留下半回退状态。
- 验证删除、截断和正常分支回退仍执行现有后缀清理与修订恢复，并与原地编辑路径严格区分。

实施日期：2026-09-06

实施结果：

- 数据事务的单一 `binding` 继续表示故事归属位置；新增运行时所有的 `sourceReferences`，在事务回执和每条记录 provenance 中保存实际使用的消息 ID、精确 revision、叙事来源及可见上游产物。
- Agent 与节点结束提交只能继承工作流运行已登记的来源；可信代码可用 `sourceMessageIds` 从当前可见正文中缩小到精确来源，未知、重复或越过可见回合的 ID 会被拒绝。
- 新增只读影响查询，可按消息与 revision 返回涉及的批次、工作流和已安装模块目标，并明确提示无法追踪来源的旧回执；检查不会修改任何权威数据。
- 原地编辑消息不会自动失效、回滚、级联重跑或阻塞其他模块。用户可把影响查询返回的批次和模块范围交给现有的显式工作流启动入口；具体模块的受控修复按钮由 INFRA-005 的声明式 `workflow-controls` 承载。
- 叙事记忆多轮归档仍绑定覆盖范围的最后一条消息，同时只把本次参考与归档正文的全部精确消息修订登记为来源。

验证结果：事务来源、provenance、只读影响查询、权限过滤、可信代码来源校验、记忆模块多轮来源选择和 Web API 测试均已通过。

补充实施（2026-09-07）：

- 公共运行时增加会话级只读完整性诊断，比较当前权威消息revision与已授权模块事务实际保存的来源revision，按模块和具体回合返回“来源已修订”提醒；旧事务缺少来源时只报告无法核验，不推定记录错误。
- 诊断不会改变记录状态、自动回滚、自动重跑或阻塞下一轮。模块可以用受控覆盖记录确认用户已经在当前revision下复核某一范围；是否修复始终由用户决定。
- 叙事记忆新增用户指定回合范围的repair/supplement两级工作流。提醒可以预填范围，用户也可在没有诊断问题时主动补档；成功处理后保存覆盖记录，并只在覆盖区间连续时推进自动归档前沿。

## INFRA-005：声明式交互模块前端区域

状态：**已实施**

确认日期：2026-09-06

规划确认日期：2026-09-06

来源模块：叙事记忆与记录模块设计

涉及系统：功能模块前端视图协议、Pi RP Web 模板、统一数据查询与提交接口、工作流前端控制

确认的升级内容：

- 在现有只读模块展示区域之外，增加受公共运行时控制的声明式交互区域，首批包括 `record-browser`、`settings-form` 和 `workflow-controls`；不允许模块注入或执行任意前端脚本。
- `record-browser` 支持按已声明的模块、集合、记录类型与前端视图进行分页查询、筛选、排序、记录详情展示和修订历史查看，避免大型集合被一次性载入前端。
- `settings-form` 根据模块声明的可编辑字段生成表单，通过统一数据服务、数据契约校验和 `expectedRevision` 提交修改；浏览器不得直接编辑权威文件。
- `workflow-controls` 只允许启动模块明确声明为可从前端调用的工作流，根据声明的参数结构生成表单，并展示运行、等待、失败、取消与完成状态。
- 为上述区域提供受控后端接口。每次查询、修改和工作流启动都必须经过模块声明、前端授权视图、能力范围、查询预算和运行时校验，不能把前端可见性等同于 Agent 权限或模块数据所有权。
- 保持前端区域与 Agent 视图相互独立。模块负责声明集合、筛选项、字段顺序、显示标签、默认状态、可编辑设置和可调用工作流；公共前端只负责通用交互、呈现与安全路由。

对具体模块的简化：

- 叙事记忆模块可以声明完整记录浏览器，分页查看实体、关系、事件、事件摘要、认知、知情群体、支持记录、来源捕获及其历史修订，不必把全部记录一次性塞入 `GET /api/modules` 的静态数据。
- 记忆模块只需声明冷却、保护轮数、压缩阈值、模型绑定等设置字段，以及手动归档、压缩和维护工作流入口，不必在共享 Web 运行时中硬编码专用表单与接口。
- 其他拥有大量数据、调试页面、运行参数或手动维护工作流的全局模块可以复用同一机制，减少模块专用 API 和共享前端分支。

不升级时的模块路径及代价：

- 使用现有静态 `frontend-view.json` 区域一次性返回并渲染全部集合，随着剧情增长会产生显著的加载、内存和响应体压力，也难以提供修订历史、复杂筛选和安全修改。
- 或在 Pi RP Web 模板中为记忆模块硬编码专用 API、页面和操作逻辑，使共享运行时直接依赖具体模块 ID、记录类型和业务字段；其他大型模块仍需重复实现。
- 允许模块自带任意前端脚本虽然可以绕过限制，但会扩大代码执行、权限绕过和兼容风险，因此不采用。

兼容与迁移：

- 新区域类型和后端接口均为增量能力；现有 `markdown`、`json`、记录列表和生图等区域继续按原方式工作，既有模块无需迁移。
- 只有显式声明新区域并配置相应前端授权的模块才获得交互能力。全局模块的源包升级不会自动修改已经复制进卡中的模块。
- 实施时需要为未知区域类型保持当前安全降级行为，并明确分页游标、筛选字段、修订历史和工作流参数的协议版本。

安全边界：

- 模块不能通过前端声明获得任意文件、任意集合、任意视图或任意工作流访问；所有目标必须在模块契约和前端授权中明确列出。
- 前端查询只返回专门声明的前端视图，不复用或扩大普通 RP、检索 Agent、归档 Agent或维护 Agent 的视图权限。
- 所有持久修改由后端构造或验证统一数据变更批次，检查 `expectedRevision`、字段范围与能力；浏览器不能直接写权威文件或提交未声明操作。
- 工作流按钮只能启动静态允许的工作流，用户输入只能填入声明参数，不能动态选择任意节点、能力、模型权限或输出路径。
- 完整记录与历史修订可能包含幕后信息；模块必须能够标注剧透区域，并由卡的本地用户主动进入，但不能因此对无权调用者开放隐藏数据。

验证要求：

- 覆盖大集合分页、稳定排序、组合筛选、空结果、翻页期间并发写入和修订历史读取，确认不会遗漏、重复或一次性加载全库。
- 验证查询视图、集合、字段和预算边界，拒绝未声明模块、集合、视图、筛选字段及越权历史访问。
- 验证设置修改的正常提交、校验失败、旧修订冲突、重试和并发编辑；确认浏览器不会直接修改权威文件。
- 验证工作流的允许与拒绝列表、参数校验、实例去重、运行状态、取消、失败恢复，以及与阻塞型工作流状态的协作。
- 验证现有前端区域和模块保持兼容，并验证未知或配置错误的新区域能够安全报错而不影响正常游玩页面。

实施日期：2026-09-06

实施结果：

- 功能模块前端协议新增 `schemaVersion: 2` 声明式交互区域，只支持公共运行时实现的 `record-browser`、`settings-form`、`workflow-controls` 与只读 `integrity-alerts`，不执行模块提供的任意前端脚本；原有 `schemaVersion: 1` 静态区域继续兼容。
- 2026-09-07 增加只读 `integrity-alerts` 区域：它可以显示来源revision异常和模块声明的回合覆盖缺口，并且只能把范围预填到另一个 `workflow-controls` 已静态授权的工作流；提醒不能自行取得写权限或启动修复。
- 记录浏览器通过稳定序号与记录 ID 组成的 keyset cursor 分页，仅允许使用模块契约声明的 collection、record type、frontend view、索引筛选和预算；详情修订历史独立分页。翻页期间新增或修订记录不会导致已访问页重复或遗漏既有结果。
- 设置表单只呈现模块声明的字段。后端根据字段声明构造统一数据变更，执行 schema、能力与 `expectedRevision` 校验；浏览器不能直接提交任意变更批次或修改权威文件。
- 工作流控制只允许启动区域静态白名单中的后台工作流，并按声明校验参数；运行状态、失败、取消和阻塞下一轮状态继续由公共工作流引擎管理，前端声明不能扩大节点权限、模型权限或输出目标。
- Pi RP Web 新增受控的分页查询、修订历史、设置读写和工作流启动接口。`GET /api/modules` 对 v2 模块不再一次性载入全部集合；未知、非法或越权区域配置在加载或调用边界被拒绝，不影响普通游玩页面。
- 叙事记忆模块已采用 v2 前端，分页开放实体、关系、事件、认知、知情群体、支持记录与来源捕获七类完整归档记录及历史，提供常用归档/检索/压缩设置，并开放手动归档、压缩与限定维护工作流入口。

验证结果：稳定分页与并发新增/修订、历史分页、区域声明校验、Web 后端接口、设置乐观并发、工作流白名单、旧版区域兼容、记忆模块包校验和前端脚本语法检查均已通过。

## INFRA-006：代码节点输出驱动条件路由

状态：**已实施**

确认日期：2026-09-06

实施日期：2026-09-06

来源模块：叙事记忆与记录模块

涉及系统：Workflow v2 定义、工作流运行时、卡包校验器

已实施内容：

- `code` 节点可以声明一个安全字段名 `routeFromOutput`。代码输出对象中该字段的安全ID值会成为节点 route，供既有 `conditions` 选择下游分支。
- 其他节点类型不能声明 `routeFromOutput`；非法字段名或非法运行时 route 会使节点失败，缺失或空值不会匹配任何指定 route 的分支。
- 叙事记忆归档与压缩工作流使用该能力：冷却、保护范围或压缩阈值不满足时，后续Agent和提交节点直接标记为跳过，不再进行无意义模型调用。

兼容与安全：

- 未声明 `routeFromOutput` 的既有工作流保持原有行为；原有 `gate` 和 `conditions` 语义不变。
- route只能控制工作流中已经静态声明的条件分支，不能增加节点权限、模块能力、视图、预算或输出路径。
- 根运行时模板升级不会自动传播到已转换卡；导入依赖此能力的模块时需要核对目标卡运行时版本。

验证结果：

- 工作流规范化测试覆盖合法代码路由字段与非法字段拒绝。
- 叙事记忆工作流测试覆盖 `skip` route 令全部下游模型节点跳过，以及 `archive`、`compress` 路由正常保留。
- 当前卡包校验器同步验证字段类型、节点类型、条件来源、route和状态值。

实施授权：用户已明确确认并完成实施。

## INFRA-007：可信代码提交的历史消息绑定

状态：**已实施**

确认日期：2026-09-06

实施日期：2026-09-06

来源模块：叙事记忆与记录模块

涉及系统：统一数据代码节点接口、事务提交上下文、消息绑定校验

已实施内容：

- 可信 `code` 节点可以调用 `data.submit(batch, {binding:{turn,messageId}})`，把延迟生成的整个批次绑定到一条较早的可见消息。
- 运行时要求目标消息仍存在于当前权威正文、记录回合与请求回合完全一致，并且不超过工作流的 `visibleThroughTurn`；任一条件不满足即拒绝提交。
- 叙事记忆延迟归档用该能力把多轮归档批次绑定到实际覆盖范围的最后一轮，而不是错误绑定到触发归档时受保护的更新回合。

兼容与安全：

- 未传入显式 binding 的代码节点继续使用原有默认绑定；Agent工具和节点结束自动提交不能选择历史消息。
- 历史绑定不改变统一变更批次、能力、expectedRevision、原子事务、后缀删除或修订恢复语义。
- 代码不能绑定不存在、已不可见、回合不匹配或位于未来的消息，也不能通过此接口登记任意来源集合。

验证结果：

- 单元测试覆盖成功历史绑定、默认绑定、未知消息、回合不匹配、越过可见范围和额外字段拒绝。
- 叙事记忆归档准备测试覆盖保护回合下的实际最后消息选择，提交代码使用同一回合和消息ID。

实施授权：用户已明确确认并完成实施。

## INFRA-008：通用随机掷骰服务

状态：**已实施**

确认日期：2026-09-10

实施日期：2026-09-10

来源需求：通用 RP 随机判定

涉及系统：Pi Agent 工具、代码节点运行时服务、Workflow v3 校验、工作流过程审计

已实施内容：

- 公共运行时提供统一结构化骰子核心，支持一至十组骰子、合计最多一百颗、整数修正值、用途说明和逐颗结果，使用 Node `crypto.randomInt()`。
- 任意 Agent 可由其有效配置显式获得 `rp_roll`；任意顶层或模块 `code` 节点可声明 `runtimeServices: ["random"]` 并调用同语义的 `services.random.roll()`。
- 每次请求必须提供逻辑 key。第一次结果按工作流 run、节点和 key 原子写入审计文件；完全相同的节点重试复用原结果，参数冲突或并发冲突失败。

兼容与安全：

- 既有 Agent 和代码节点不会自动获得随机能力；Agent 工具白名单与代码节点运行时服务声明相互独立。
- 随机审计记录属于运行时过程信息，不自动进入模型上下文，也不成为统一数据权威。需要持久化的游戏事实必须由所属模块显式提交。
- 未声明 `runtimeServices` 的既有 Workflow v3 保持兼容；未知服务、非代码节点声明、重复服务和越界骰子参数均被拒绝。

验证结果：随机边界、多骰组求和、幂等重放、参数冲突、工作流服务授权和卡包结构校验均由自动化测试覆盖。

实施授权：用户确认公共随机工具方案，并明确要求同时支持任意获授权 Agent 与代码节点。

## INFRA-009：受控文档上下文快照与跨工作流交接

状态：**已实施**

确认日期：2026-09-11

实施日期：2026-09-13

来源模块：世界叙事统筹模块设计

涉及系统：Workflow v3 文档工作区、模块调用输入、顶层触发产物路由、工作流运行时

确认的升级内容：

- 增加由运行时生成的只读 `document-workspace-snapshot` 产物，用于冻结一个节点已经获得授权的文档上下文，而不是扫描或复制该节点的整个物理工作目录。
- 快照只包含运行时已经登记到文档工作区的资料及显式物化的上下文，可保留路径、文件种类、哈希、读取策略、权威层级、适用阶段、来源消息修订和可见回合边界；隐藏推理、进程记录、工具日志、未声明临时文件、凭据和运行时秘密不得进入快照。
- 允许工作流作者在精确的模块调用授权中静态声明：调用目标可以接收当前调用节点的文档上下文快照。普通 Agent 不能通过调用参数扩大快照范围或传入任意目录。
- 允许顶层来源工作流声明一个明确命名的文档上下文快照产物，并由 `after-workflow` 或其他适用的既有顶层触发绑定将该确切产物映射到目标工作流的声明输入；顶层触发本身仍不隐式继承工作区。
- 对需要阻塞下一轮的后置工作流，协调完成快照冻结、触发实例登记和回合阻塞建立，避免来源工作流完成后已经放行下一轮、后置实例却因交接失败而未启动。
- 快照在生成后不可变；节点重试继续使用同一快照。快照属于有界生命周期的过程产物，不自动成为模块权威数据，长期可查询内容仍需由所属模块通过统一数据服务归档。
- 设置文件数量、总大小、单文件大小和目录深度上限，并继续拒绝路径逃逸、符号链接、未登记文件、种类不符和目标碰撞。

对具体模块的简化：

- 世界叙事统筹模块的前置导演可以在正文 Agent 动态调用时直接取得该节点已获授权的完整文档上下文，不需要再次调用记忆检索，也不需要正文工作流为每类资料编写重复打包逻辑。
- 正文工作流可以在完成后把本轮实际使用的资料、玩家输入、检索结果和最终发表正文冻结为一个显式快照，交给后置导演复盘；后置导演不依赖隐式跨工作流工作区，也不必重新检索。
- 其他需要把一次复杂分析的完整授权材料交给校对、审核、归档或后处理工作流的模块可以复用同一能力。

不升级时的模块路径及代价：

- 每一种正文工作流都需要增加模块专属代码节点，逐项收集、复制和重新生成 `document-set`；动态模块调用结果和检索结果必须分别适配，新增资料来源时还要同步修改打包逻辑。
- 顶层后置触发仍需额外的持久化中转与清理机制，容易遗漏正文 Agent 实际看过的资料、丢失来源元数据，或者把临时产物错误当成跨回合权威数据。

兼容与迁移：

- 项目尚未进行实卡测试，本次按用户确认不保留旧卡兼容层、不编写会话迁移；标准与进阶正文模板直接升级为当前快照和统一回合窗口语义。
- 已复制到卡中的旧运行时不会自动获得该能力；以后导入依赖此能力的模块时必须使用当前运行时和重新生成的卡级工作流。

安全边界：

- 快照只能从运行时登记的授权文档目录表构造，不允许工作区扫描、通配符、排除列表或任意磁盘路径。
- 传递文档不传递来源节点的数据能力、工具权限或写权限；目标工作流仍受自身声明的输入、模块访问和调用授权限制。
- 只有工作流作者的静态配置可以允许当前文档上下文进入某个精确目标；Agent 输出、模块数据和运行参数不能动态扩大权限。
- 快照需要保留来源和叙事层级，不得把有条件、传闻性或角色视角材料提升为无来源的作者事实。

验证要求：

- 覆盖同节点动态模块调用、顶层工作流完成后触发、动态检索结果、最终正文与候选草稿区分、重试复用和产物清理。
- 覆盖未授权文件排除、权限不提升、缺失文件、超限、路径逃逸、符号链接、种类不符和目标碰撞。
- 覆盖阻塞型后置工作流在快照或实例登记失败时的失败处置，确保不会错误放行下一轮，也不会形成不可恢复的永久阻塞。
- 验证不同会话、分支、消息修订和可见回合边界之间的隔离。

验证结果：工作流规范化、快照白名单复制、文件哈希、数量/大小/深度边界、重试复用、顶层触发文档映射、目录交接和模块调用限制已纳入 Node 自动化测试；尚未执行真实模型实卡端到端验证。

实施授权：用户于2026-09-13确认执行。

## INFRA-010：模块内部工作流的声明式写入锁域

状态：**已实施**

确认日期：2026-09-11

实施日期：2026-09-12

来源模块：世界叙事统筹模块设计

涉及系统：Module v6 清单、Workflow v3 契约与校验、工作流调度运行时、统一数据能力覆盖检查

确认的升级内容：

- 将当前“同一模块所有 `module-internal` 工作流共享一条串行通道”拆分为工作流实例唯一性与模块写入排他性两种独立语义。
- 模块作者可以静态声明若干逻辑写入锁域，并把每个锁域映射到一个或多个完整集合，或映射到需要排他的外部副作用资源；第一阶段不支持由 Agent、运行参数、任意文件路径或单条记录动态产生锁域。
- 每个 `module-internal` 工作流静态声明运行所需的一个或多个锁域。同一会话、同一模块内锁域有交集的工作流串行，锁域完全不相交的不同内部工作流可以并行；不同会话或不同模块互不阻塞。
- 未声明细分锁域的现有模块和内部工作流隐式使用整模块锁 `module:*`，并与该模块的所有细分锁域冲突，完整保留现有行为。
- 同一种内部工作流仍保持单实例。需要多个锁域的工作流必须在启动前一次性申请全部锁，由运行时规范化排序并原子取得，不允许运行中追加锁域。
- 锁覆盖权威读取、分析、修改和提交的完整读改写生命周期，而不只覆盖最后的文件写入。等待重试或模型选择的实例继续持锁；成功、显式跳过、已处置失败或取消进入终态后释放，运行时恢复时根据持久化实例状态重建锁占用。
- 为冲突锁域提供稳定、公平的等待队列，避免低频维护工作流长期饥饿，并在运行状态中公开锁占用与等待原因。
- 校验器必须保证内部工作流的全部可写集合、自定义操作潜在写入集合和外部副作用能力均被其声明锁域覆盖；锁域本身不授予能力，也不能扩大模块所有权。
- 第一阶段只放宽工作流读取、模型分析和生成阶段的并发。统一数据最终提交继续保持现有会话级短暂串行，并继续执行 `expectedRevision`、幂等批次、原子提交、冲突拒绝和回滚规则。

对具体模块的简化：

- 世界叙事统筹模块的后置导演可以维护活跃导演状态，深度推演记录器可以同时归档独立的规划提案，不必因共享整模块串行通道而让昂贵模型阶段互相等待。
- 当后置导演需要吸收规划提案时，可以一次取得活跃状态与规划收件箱两个锁域，原子完成读取提案、更新当前状态和标记提案已处理。
- 其他拥有多个独立数据生命周期或独立外部副作用资源的模块可以复用统一并发原语，不需要分别建设模块私有锁、临时中转和轮询机制。

不升级时的模块路径及代价：

- 深度推演可以只生成文档，但记录工作流仍必须等待所有同模块内部工作流结束；或者由日常维护工作流兼任提案归档与吸收，扩大职责并增加失败恢复逻辑。
- 未来每个需要独立写入分区的模块都要接受不必要的模型等待，或重复实现模块专属中转和协调代码。

兼容与迁移：

- 未采用锁域声明的模块与工作流继续整模块串行，现有会话数据无需迁移。
- 旧模块可以按需逐步拆分集合并声明锁域；升级根运行时不会自动修改已复制进卡片的模块或已安装运行时。
- 真正允许不同集合同时进行物理事务提交不属于本条范围；如未来证明提交阶段成为瓶颈，应另行设计共享事务日志、回执、模块索引、身份索引和多文件恢复的并发协议。

安全边界：

- 锁域只能由模块作者静态声明，并限定在所属模块及其明确声明的外部副作用资源内；Agent 输出和调用参数不能改变锁集合。
- 锁与能力独立：取得锁不授予读取、修改、提交或外部操作权限，拥有能力也不能绕过所需锁。
- 整模块默认锁保证既有模块不会因运行时升级而意外并发；声明覆盖不足、未知锁域、跨模块锁域或潜在写入集合不明确时校验失败。
- `module-external` 继续保持只读或纯转换语义，不通过锁域获得权威写入或不可逆外部操作能力。

验证要求：

- 覆盖旧模块整模块串行、相同锁域串行、无交集锁域并行、整模块锁与子锁域冲突、同一内部工作流单实例和不同会话/模块隔离。
- 覆盖多锁域原子取得、规范排序、等待公平性、取消、跳过、失败处置、模型选择等待和运行时重启恢复，验证没有死锁、提前释放或永久遗留占用。
- 覆盖数据能力、自定义操作写入集合、外部副作用与锁域声明的静态覆盖检查，以及 Agent 或参数尝试扩大锁域时的拒绝。
- 覆盖并行分析后的 `expectedRevision` 冲突、幂等重放、原子回滚和最终会话级提交串行，确认锁域不会削弱统一数据事务保证。

实施结果：

- Workflow v3 的 `module-internal` 工作流未声明 `writeLocks` 时继续取得整模块锁；显式声明时必须至少包含一个所属模块的集合锁。
- 同模块同集合及整模块/集合锁互斥，不相交集合锁允许并行；锁覆盖完整工作流生命周期，等待模型选择时继续持有，完成、跳过、取消、终态清理和运行恢复均正确维护锁状态。
- 模块内部工作流实例键与具体工作流绑定，不再因共享模块 ID 而无条件串行；统一数据提交仍保持原有事务串行、原子批次、幂等和 expectedRevision 校验。
- 运行时规范化与卡包校验器拒绝空锁、跨模块锁、未知集合、重复锁，以及未被锁覆盖的所属模块写能力。

验证结果：旧模块默认整模块串行、相同锁冲突、不相交集合并行、空锁拒绝、写能力覆盖和全量运行时回归测试均已通过。

实施授权：用户已确认并完成实施。

## 修订说明

本文件初建时曾直接列入叙事记忆模块讨论中发现的若干潜在公共缺口。用户随后明确了“先比较、先告知、确认后再登记”的原则，因此这些未经取舍确认的条目已经撤回，不视为顶层升级计划。后续若相关问题在具体设计中再次出现，将依照本文件流程重新提出。
