# 项目审查实施方案

创建日期：2026-09-13。

对应审查报告：[PROJECT-REVIEW-2026-09-13.md](M:/ai/SillyTavern/bobo-agent-rp/PROJECT-REVIEW-2026-09-13.md)。

## 文档使用规则

1. 按 R01—R18 逐项讨论，先解释问题，再讨论具体处理方式。
2. 每项处理方案仅在用户明确确认后写入“已确认方案”；不将审查报告中的建议自动视为已确认方案。
3. 已确认方案记录处理目标、具体改动、影响范围、兼容及迁移考虑、验证与验收标准；不适用的项目明确说明。
4. 方案确认本身不自动触发代码实施；用户已在全部问题确认后明确要求开始实施，当前代码改动及验证结果记录在“实施记录”。
5. 当前讨论默认针对根目录开发源、运行时和 Web 模板、全局模块及相应测试/文档。涉及安装运行时、已有卡片或 session 的修改，须另行明确目标和范围。

## 讨论进度

| 问题 | 状态 |
|---|---|
| R01 | 基础机制通过，完整业务链路待复验 |
| R02 | 配置及工具校验通过 |
| R03 | 已确认保留现有设计，暂不增加严格代码控制 |
| R04 | 验收不通过：生图重放与阶段恢复 |
| R05 | 部分完成：缺发布完整性检查 |
| R06 | 验收不通过：必要历史及重复传递 |
| R07 | 参数机制通过基础验证，完整调用链未通过 |
| R08 | 部分完成：关联查询仍预读全目录 |
| R09 | 验收不通过：跳过分支及触发语义 |
| R10 | 验收不通过：业务序号仍按 envelope 排序 |
| R11 | 部分完成：字段/schema/审核校验未统一 |
| R12 | 验收不通过：未知提交阶段可能重复入队 |
| R13 | 验收不通过：专用文档之外仍重复传入 request |
| R14 | 部分完成：内容版本机制未落实 |
| R15 | 验收不通过：有效预算未到达 Agent |
| R16 | 部分完成：必要指导可达性及重复说明待修复 |
| R17 | 验收不通过：必要输入遗漏与无关资料复制 |
| R18 | 验收不通过：直接提交失败未传播至工作流 |

R01—R18 已全部讨论并确认，但 2026-09-13 最终核对发现实施遗漏与组合路径故障，**总验收不通过**。具体证据、14 项缺陷及两项补充验收缺口见[最终验收报告](M:/ai/SillyTavern/bobo-agent-rp/PROJECT-REVIEW-FINAL-ACCEPTANCE-2026-09-13.md)。下方分批实施记录保留历史过程，其中“已实施”的判断以本次验收结论为准。R03 保留现有设计；R15 不新增硬性查询计数限制；固定提示与必要非聊天资料继续保留。

## 实施记录

### 2026-09-13：第一批公共交接、记忆与导演基础改动

- R01：文档描述开始区分完整交付路径与阅读入口；目录快照复制完整目录并在清单中记录入口，已增加嵌套目录回归测试。仍需补实际 Web 登记到快照的集成覆盖。
- R02：`narrative-memory-retriever` 已启用 Pi 自带 `read`。仍需完成全部文档工作区 Agent 的一致性检查。
- R05：Git 发布白名单已放行近场与广域模块完整目录；两目录已由 Git 识别。
- R06：后台触发和手动后台启动已停止默认注入完整历史正文；实际使用资料继续通过显式回合上下文、触发文档及节点资料交付。仍需完成调用参数最小化和端到端提示对比。
- R07：模块调用已拒绝未声明参数、未声明文档和基本类型错误，并有契约测试。仍需统一代码节点内部参数读取入口并移除旧透传。
- R08/R17：参考快照按所需集合读取，纯时间线不再读取五类目录；取消无消费者的重复 `snapshot.json`。仍需完成其他节点级资料选择。
- R09：压缩功能关闭时先结束，不再扫描事件。工作流前置资格判断和复用有效事件集合仍待完成。
- R14：导演归档交接已改用来源记录与内容版本形成稳定捕获 ID，确认步骤直接记录该 ID。仍需增加确认失败后的重放测试。
- R15：记忆结果组装已读取持久检索设置并执行自定义预算开关；压缩初始值和当前状态的界面说明仍待完成。
- R18：导演和故事审核的 Agent 批次共用代码包装固定协议、状态与技术 ID，代码稳定前置归档操作。仍需同步精简 Agent 输出提示并增加依赖顺序测试。

本批验证：公共工作流/快照相关测试 27 项通过；叙事记忆运行时 18 项通过；导演运行时 3 项通过；近场故事运行时 2 项通过。

### 2026-09-13：第二批节点裁剪、生图恢复与配置统一

- R02：公共执行器会在文档工作区 Agent 启动前核对其是否启用 Pi `read`；工作流定义同时校验节点声明的调用文档确实属于接口文档输入。生图提示 Agent 也已按实际文档读取需求启用 `read`。
- R04/R12：浏览器为每次用户生成或重新生成操作建立独立 `operationId`，请求和渲染记录 ID 由该身份稳定派生；普通生成与 Web 重新生成共用 `runtime/image-execution.mjs`。ComfyUI 入队后立即回调登记 `submitted/promptId`，后续完成登记使用刚读取的实际 revision；完成登记中断时按原 `promptId` 查询历史并补写，不重复生成。明确执行失败可回到 pending 后重试，无法确认的已提交任务保留 submitted 状态。
- R07：模块代码节点已统一从 `run.arguments` 读取业务参数；模块调用入口拒绝未声明参数、文档和基本类型错误。顶层工作流自己的运行 payload 仍作为顶层触发数据保留，不再作为子模块的隐式参数通道。
- R09：归档和压缩先运行资格检查，仅在 `archive/compress` 路由成立后调用参考快照工作流；禁用、冷却、保护范围或阈值不足时不会再生成无用快照。压缩资格计算与后续时间线仍存在一次重复读取，继续归入后续公共数据准备收敛。
- R13：输入解析代码生成独立 `prompt-context` 文档集，首部统一写明用途、阅读和输出规则；参考内容、画面目标、额外要求、连续性资料及模型指导只在 Agent 文档中出现一次。连接、配置摘要等执行数据留给后续代码，Agent 使用固定上下文并仅接收文档交接。
- R14：补充了“记忆捕获成功但导演确认失败”的重放测试，验证两次调用使用相同来源版本捕获 ID，并在第二次补齐确认。
- R15：新增确定性的有效预算准备节点，持久设置与获准覆盖值只解析一次，同一结果同时交给检索 Agent 和组装代码；旧的 `maxRecords/maxCharacters` 伪业务预算已从故事调用移除，深度导演改用实际六项检索预算。前端新增当前压缩运行状态区域，并明确初始化阈值不会重置当前阈值。
- R16：修正正文集成文档中要求 Agent 手工打包导演输入的旧流程；检索文档统一为自然语言 `request` 加可选 `budget` 参数，并移除另一套固定条数说明。其余模块资料首部指导仍在继续核查。
- R17：调用文档按节点 `metadata.callInputs` 显式选择，未声明文档不复制；依赖节点原始返回值仅在 `nodeOutputInputs` 明确列出时才写入文档工作区。故事创作、审核、组装和发布节点均声明最小输入范围，未使用的机器快照已取消。
- R18：前置、后置、维护、完整性修复和故事审核 Agent 只输出业务 `operations`；共用代码补齐协议版本、状态、原子策略与技术 ID，并仅在明确配置时稳定前置 archive-outbox。测试覆盖排序、`expectedRevision` 和业务 target ID 保留。

本批已通过：公共工作流、数据运行时、Web/ComfyUI、叙事记忆、导演及近场/广域故事的 92 项不重复针对性测试；另通过生图模块清单、数据契约和工作流定义规范化，以及相关 JavaScript 语法检查和 `git diff --check`。未调用真实模型或真实 ComfyUI，未修改已有卡片、`play/` 或 session。

### 2026-09-13：第三批交付集成、资料复用与叙事机械统一

- R01：Web 触发文档登记直接复用公共描述转换，并增加“真实登记→冻结快照”集成测试；资料目录的交付路径、阅读入口和嵌套文件在整条链路中保持一致。测试同时发现并修复触发文档 ID 被重复添加 `trigger` 前缀的问题。
- R06：提示上下文组装提取为可测试函数。固定系统提示、Agent 提示和卡片固定资料继续保留；最近聊天回合只在前台工作流中隐式组装，后台工作流只取得显式调用参数、触发文档和节点文档。
- R09：压缩资格节点复用其唯一一次有效事件读取，直接生成后续压缩 Agent 使用的时间线和根 `DOCUMENTS.md`；删除重复参考快照调用。禁用或不满足阈值时不生成资料。
- R10：公共数据运行时新增统一分页遍历；记忆、近场、广域和导演模块的数据助手在公共运行时存在时统一委托给它。记忆目录的 view 转换继续只由 `prepare-catalog` 的 `catalogEntry` 提供，参考快照直接复用。
- R11：近场与广域故事的候选校验、发布、近期导出和上下文准备合并为同一参数化机械实现。技能资产保存唯一规范源，两个可独立安装模块携带生成副本；同步脚本和测试保证副本不分叉。
- R16：所有本次新增或调整的多文件资料在根 `DOCUMENTS.md` 首部说明用途和阅读顺序；模块调用与集成说明已与当前参数、自动快照和文档输入机制对齐。

本批增加了 Web 触发资料集成、压缩单次读取复用、后台提示历史隔离及故事机械同步测试。当时记录的 JavaScript 合并回归为 147 项通过，生成副本检查和 `git diff --check` 通过。原“13 项 Python 测试通过”缺少有效运行依据，已在最终验收中撤回并更正。仍未调用真实模型或真实 ComfyUI，也未修改已有卡片、`play/` 或 session。

### 2026-09-13：最终核对验收

总验收不通过。重新运行现有 JavaScript 测试为 147/147 通过；使用实际 Python 可执行文件发现一处旧接口断言，修正测试兼容字符串/对象调用声明后为 16/16 通过。但真实引擎、完成钩子、提示资料组装与数据排序等补充探针复现了原测试未覆盖的故障，不能据现有测试全绿标记方案完成。业务代码缺陷及复验要求详见最终验收报告。本次只修正上述测试断言并更新验收文档。

## 已确认方案

### R01：区分资料交付范围与阅读入口

确认日期：2026-09-13。当前验收状态见顶部进度表及最终验收报告。

**处理目标**

已选定交付的资料必须完整到达下游，接收者从说明入口开始按需阅读，目录内相对引用保持有效。

**具体处理方案**

1. 在运行时文档描述中分别表达交付文件/目录路径与阅读入口；资料目录的阅读入口通常为其内部的 `DOCUMENTS.md`，不能用入口路径替代交付目录路径。具体字段命名在实施时按现有协议确定。
2. 工作区索引向 Agent 展示阅读入口；冻结快照按已声明的交付范围复制文件或目录，并保留目录内部结构和相对引用。
3. 前置导演自动调用快照与正文结束快照采用同一规则；静态上游交接和动态调用结果采用一致的文档描述语义。
4. 资料打包、复制和快照均由代码完成，不要求 Agent 手动重组目录。
5. 保留现有显式交接范围、路径边界及快照限制。完整交付指已经选定的资料范围；针对不同消费者如何进一步筛选内容，留待相关问题讨论，不在 R01 中扩大为新的筛选机制。

**影响范围**

根目录公共运行时模板中的文档登记、工作区索引和快照代码，以及对应的协议说明、测试；如现有工作流声明需要适配，仅调整与本项直接相关的根目录模板声明。

**兼容与迁移**

普通文件仍按文件交付；目录按目录交付，并保留内部阅读入口。实施时核对全部调用处，避免出现声明为目录而路径指向索引文件的混用。此项不涉及权威业务数据 schema 迁移，也不自动修改安装运行时、已有卡片、session 或历史快照。

**验证与验收标准**

- 从实际工作区文档登记逻辑生成描述，再调用快照器，不能只用手工构造的正确目录描述测试快照器。
- 对含根 `DOCUMENTS.md`、正文文件和嵌套子目录的资料包，冻结后可从阅读入口沿相对路径读到全部选定文件。
- 前置自动快照、正文结束快照以及静态/动态资料输入均覆盖验证；普通文件交付正常。
- 未声明交付的相邻文件不被带入，现有路径安全、大小限制与快照重用行为通过回归验证。

### R02：启用 Pi 自带文件读取工具并检查配置一致性

确认日期：2026-09-13。当前验收状态见顶部进度表及最终验收报告。

**处理目标**

需要读取交付文档的 Agent 具备实际读取能力，任务要求与工具配置一致。

**具体处理方案**

1. 在 `narrative-memory-retriever` 的工具白名单中启用 Pi 自带的 `read`；复用现有 Pi SDK 工具，不另行开发文件读取工具。
2. 保留现有文档工作区交付流程及记忆检索业务职责，Agent 从 `WORKSPACE-DOCUMENTS.md` 进入并读取交付资料。
3. 核查根目录源中其他使用文档工作区的 Agent，对同类“需要读文档却缺少读取工具”的配置遗漏一并修正；不对无文件读取需求的 Agent 普遍加权。
4. 增加配置一致性检查，核对需要读取交付文档的节点与实际解析出的 Agent 工具集，防止后续调整流程时再次漏配。

**影响范围**

根目录记忆检索 Agent 配置、核查发现的同类 Agent 配置，以及对应配置检查和测试。复用现有运行时工具注册机制。

**兼容与迁移**

不改变工具名称、调用接口或业务数据结构；无需数据迁移。不自动更新安装运行时、已有卡片或 session。

**验证与验收标准**

- 检索节点实际创建会话时的工具集中包含 Pi 的 `read`，原有数据查询工具仍可用。
- 已交付的工作区索引和目录资料可通过该工具读取；与 R01 的交付完整性验证衔接。
- 配置检查能识别已知的缺少读取工具场景，并接受修正后的配置；不要求无文档读取任务的 Agent 启用 `read`。

### R03：保留 Agent 灵活把控维护范围的设计

确认日期：2026-09-13。状态：已讨论并确认保留现状，暂不实施代码收紧。

**决定与理由**

用户明确说明：记忆模块复杂性高，原设计就是由 Agent 灵活把控维护工作，本阶段暂不考虑增加严谨的代码控制。审查发现的“提交端未严格绑定本次目标范围”作为已知设计取舍保留，不列为本轮待修复项。

**处理范围**

- 保留由 Agent 理解维护任务、判断关联修改和把控范围的方式。
- 本项不新增目标记录硬白名单、不按维护任务类型增加严格动作限制，也不引入关联修改的强制二次确认流程。
- 保留已有的模块/集合权限、数据合法性、revision 与事务校验；本决定不要求移除这些现有机制。
- 当前无代码修改、数据迁移或新增测试任务。将来如需调整，由用户重新提出或确认。

### R04：以生成操作身份统一生图去重

确认日期：2026-09-13。当前验收状态见顶部进度表及最终验收报告。

**处理目标**

区分用户主动发起的新生成与同一次操作的重复到达，允许相同内容再次生成，同时避免请求重发导致重复执行。

**具体处理方案**

1. 用户每次明确发起生成操作时建立新的请求 ID，即使剧情、配置和额外要求完全相同，也允许产生新的图片。
2. 同一次操作因网络重发、工作流重试等重复到达时沿用原请求 ID；ID 不得在每次传输或内部重试时重新生成。
3. Web 入口与模块内部传递、使用同一个请求身份，取消两处各自按内容计算并决定是否执行的去重规则。内容相同不作为阻止新生成的依据。
4. 在提示词 Agent 调用前检查请求状态：已完成则返回已有结果，执行中则复用当前任务，失败则依据已完成阶段处理重试，避免无条件重做已完成步骤。
5. 失败恢复、阶段状态及统一执行服务的具体实现与 R12 一起细化；R04 先确定上述请求身份与重复处理语义。

**影响范围**

根目录 Web 生图入口、顶层生图工作流模板、ComfyUI 模块的请求传递、去重及状态检查逻辑，以及对应测试和接口说明。

**兼容与迁移**

不再仅因存在内容相同的历史请求而阻止新请求。实施时明确请求身份字段及其与现有请求记录 ID 的关系；历史记录保留，兼容读取方式结合 R12 的状态方案确定。不自动迁移或修改已有 session。

**验证与验收标准**

- 修改额外要求（例如白天改夜景）可生成新的请求；内容完全相同但明确再次生成也可建立新请求。
- 同一次操作重复到达或执行中重试时不会并行启动重复的 Agent/生成任务。
- 已完成请求重复到达时返回已有结果，不再次调用提示词 Agent 或 ComfyUI。
- 失败请求可进入约定的重试路径，不会因为旧记录存在而永久被判重复；分阶段恢复细节随 R12 补齐验收。

### R05：补齐近场与广域模块的发布白名单

确认日期：2026-09-13。当前验收状态见顶部进度表及最终验收报告。

**处理目标与方案**

保留项目现有的 Git 发布白名单机制，补充放行 `global-modules/local-scene-narrative/` 与 `global-modules/world-scope-narrative/`。核对两模块的代码、schema、静态素材、清单和测试均可被正常收录，并在发布检查中核对项目声明提供的模块与实际提交的模块是否一致，防止后续新增模块遗漏。

**影响范围与兼容**

仅涉及根目录 `.gitignore`、相关发布检查及说明，不改变模块功能、业务数据结构或已有卡片。无数据迁移。不因本方案自动执行 Git 提交或发布。

**验证与验收标准**

- 两模块的发布源文件不再被 Git 忽略，可通过常规添加和提交流程收录。
- 核对发布文件清单，确保包含模块运行与验证所需的文件，而非仅放行 `module.json`。
- 原有本地卡片、会话和个人设置的忽略规则继续有效。
- 发布检查能够发现“项目声明提供模块，但实际提交内容缺少该模块”的情况。

### R06：按任务组装历史资料，消除完整历史的隐式透传与重复注入

确认日期：2026-09-13。当前验收状态见顶部进度表及最终验收报告。

**处理目标**

模块按自身任务取得所需聊天资料，避免入口额外透传完整历史或在多处重复展开同一内容；保留各模块对于固定提示词及其他非聊天资料的需求。

**具体处理方案**

1. 取消公共后台入口默认携带完整聊天历史的行为，由模块或工作流明确选择所需范围。确实需要完整历史的任务可显式声明并取得完整历史。
2. 用代码明确组装调用参数，传递必要业务参数和文档引用，替代整个运行 payload 的透传。技术运行状态不因存在于 payload 中就自动成为 Agent 输入。
3. 同一份聊天资料只交付一次；已经生成资料文档时，提示词提供入口和使用说明，不同时展开全文、包含全文的调用参数及另一份相同资料。
4. 保留冻结能力，确保后台任务依据启动时选定的资料工作；移除默认全历史注入不等于改为读取不断变化的实时历史。
5. 明确保留 Pi 系统提示、模型首尾提示、Agent 职责提示、节点任务提示，以及各节点原本启用的卡片固定上下文。原本显式关闭固定上下文的节点也继续尊重该配置。
6. 模块所需的静态资料、创作规则、模型指导、玩家/角色资料、记忆结果及其他非聊天输入，按各自既有需求继续准备和交付。若目前混在被透传的 payload 中，实施时先识别并迁入明确的参数或文档通道，不能随全量透传一起丢弃。
7. 保留记忆维护由 Agent 进行语义判断和灵活处理的设计，不借此引入 R03 已决定暂不实施的严格范围限制。

**影响范围与兼容**

根目录公共后台启动与提示组装代码、使用 `forwardPayload` 的归档/范围修复/维护/压缩/生图入口，以及其实际消费参数的模块准备代码、对应文档和测试。实施前逐条核对消费需求，避免取消透传后遗漏必要参数。无权威业务数据迁移，不自动修改已安装运行时、已有卡片或 session。

**验证与验收标准**

- 端到端覆盖顶层入口、模块调用和最终 Agent 提示组装；选定回合范围时，不再额外混入未选定的完整历史。
- 同一历史全文不在固定历史片段、参数 JSON 和资料正文中重复注入；文档入口仍有清楚的使用说明。
- 显式需要完整历史的任务仍能取得完整且冻结的资料。
- 对比改动前后的有效提示和交付资料清单，各模块必需的固定提示、静态资料、模型指导及其他非聊天输入仍完整可用；原有启用/关闭配置保持生效。
- 选择窗口、额外要求及其他业务参数仍正确到达消费者；运行技术字段不再被无条件加入 Agent 上下文。

### R07：统一模块调用参数入口并使接口声明与实现一致

确认日期：2026-09-13。当前验收状态见顶部进度表及最终验收报告。

**处理目标与方案**

1. 调用方统一通过 `arguments` 传递业务参数；运行时为代码节点提供明确、统一的参数读取入口，清理在 `payload`、`payload.request`、`payload.call.arguments` 等位置间尝试读取的回退逻辑。具体内部入口命名在实施时确定。
2. 每个模块接口声明实际消费的参数名称、基本类型及必填性，调用方与接收方使用相同约定。
3. 公共调用入口检查未声明参数、缺失的必填参数和基本类型错误，错误信息指出具体接口及参数位置。
4. 保留模块内部业务结构的自由度：可声明允许扩展的对象；自然语言任务和复杂维护内容仍由模块及 Agent 理解，不要求公共层严格穷举所有业务字段。
5. 同步调整根目录中现有调用方、接收方、接口说明及测试，不能仅收紧入口校验而留下不兼容调用。确有过渡适配需要时集中于明确边界，不继续在各模块分散保留多位置读取方案。
6. 与 R06 配合，业务参数和技术运行状态分开；不引入 R03 已决定暂不采用的记忆维护目标范围或动作硬限制。

**影响范围与兼容**

根目录公共工作流调用规范化与代码节点参数交付、全局模块和工作流模板中的调用/读取逻辑、相应接口文档和测试。保留文本输入与文档输入各自用途，不强制将所有内容转换成结构化业务参数。无需权威数据迁移，不自动修改已安装运行时、已有卡片或 session。

**验证与验收标准**

- 现有根目录模块调用链经调整后能通过声明接口正确传参，必要业务参数无遗漏。
- 未声明参数、缺少必填参数及错误基本类型在入口明确失败，不静默丢弃或误用默认值。
- 已声明的可选参数和允许扩展的业务对象正常工作；自然语言请求可原样传递。
- 代码节点使用统一参数入口，技术运行状态不会借多位置回退混入业务参数。
- 接口说明、Agent 可见的调用签名与实际实现一致。

### R08：按请求读取记忆快照所需数据

确认日期：2026-09-13。当前验收状态见顶部进度表及最终验收报告。

**处理目标与方案**

在保持返回内容和既有语义一致的前提下，减少参考快照生成过程中无关目录读取及重复处理。

1. 仅请求时间线时，只读取计算有效时间线所需的事件与事件摘要资料，不预先读取实体、关系、认知和知情群体的全部目录。
2. 请求指定集合、记录或筛选范围时，优先使用集合、ID 或已有索引缩小候选范围，再完成剩余筛选。仅对不改变结果语义的条件提前筛选。
3. 请求实体关联资料时，通过已有关系索引取得所需记录，省去无关全目录预读。
4. 明确请求完整目录时继续完整读取并交付，不因优化缩减 Agent 所需资料。
5. 同一次快照同时请求目录和时间线时，复用共同读取的数据，避免重复查询与组装。
6. 保留“摘要替代其覆盖事件”的有效时间线计算规则；不能因提前筛选漏掉覆盖关系，从而把已被摘要覆盖的事件误当成有效事件。

**影响范围与兼容**

根目录 narrative-memory 的参考快照构建、目录准备及相关查询辅助代码、测试和说明。对外请求与结果的业务语义保持不变，参数入口与 R07 的统一调整衔接。不新增跨运行缓存机制，不涉及权威数据迁移或已有 session 修改。

**验证与验收标准**

- 只请求时间线时不读取其余四类无关目录；指定集合、ID 和实体关联请求不再无条件预读全部目录。
- 完整目录、筛选目录、有效时间线及组合请求的结果与既有语义一致，包括空结果及嵌套摘要覆盖等边界。
- 覆盖关系所需记录不能因筛选被遗漏；分页完整性和现有查询权限保持有效。
- 记录模拟数据服务的调用，验证减少了无关读取，并验证同一次组合请求对共同数据的复用。

### R09：先判断任务是否需要执行，再准备资料

确认日期：2026-09-13。当前验收状态见顶部进度表及最终验收报告。

**处理目标与方案**

1. 调整归档、压缩工作流：先由代码检查启用状态和执行条件，不需执行时正常结束；满足条件后再生成后续 Agent 所需参考快照。
2. 判断压缩阈值所需的有效事件读取仍予保留；将已经读取、计算的有效事件资料复用于后续快照，避免重复查询和计算。与 R08 共用目录/时间线语义，不新建另一套有效事件计算规则。
3. 对导演私有资料、作者静态资料等准备步骤核查实际依赖，仅将没有数据依赖或锁冲突的步骤并行执行；消费者等待必需资料全部就绪。
4. 保留归档冷却、保护回合、压缩阈值、手动触发规则以及两级归档职责，不改变实际业务处理顺序。
5. 不需要执行时继续由代码路由跳过 Agent，不让 Agent 为判断是否跳过或输出空结果而额外运行。

**影响范围与兼容**

根目录记忆归档/压缩工作流及准备代码、经核实可独立执行的资料准备节点、相应测试和流程说明。保持已有锁、可见回合和一致性边界；跨节点资料复用通过明确交付实现。不涉及权威业务数据迁移或已有 session 修改。

**验证与验收标准**

- 功能关闭、未到阈值或处于冷却/保护范围时，不执行无关参考快照生成，也不调用 Agent；工作流正常返回。
- 满足条件时，Agent 取得完整所需资料，归档范围、有效事件集合及提交结果语义保持一致。
- 压缩条件判断与快照复用同一次准备所得的有效事件资料；保留提交时必要的有效性和版本检查。
- 独立准备步骤可并行，消费者不会提前启动；存在依赖或锁冲突的步骤仍遵守执行约束。

### R10：统一分页遍历与记忆目录转换

确认日期：2026-09-13。当前验收状态见顶部进度表及最终验收报告。

**处理目标与方案**

1. 对明确需要完整结果的查询，使用共用分页遍历机制，根据 `nextCursor` 继续读取，不能把单页结果当作完整集合。
2. 记忆、导演等调用者保留各自可配置的每页条数和字符预算；统一的是遍历与结束判断，不强制统一业务预算。
3. 近场、广域故事的上一篇选择及下一个业务序号计算，按目标系列和对应业务序号明确排序，取得最小必要结果。核对该排序字段与运行时 envelope 序号的区别，确保使用正确索引。
4. 需要完整系列目录时仍完整分页读取；不将所有查询普遍改成全量读取。
5. 记忆目录 view 到内部字段的转换保留一个实现，目录准备和参考快照构建共同复用，与 R08 的按请求读取方案衔接。

**影响范围与兼容**

根目录公共分页辅助能力及其记忆/导演调用者、近场与广域相关故事查询、记忆目录转换代码，以及测试和说明。共用能力的具体代码位置在实施时按模块安装边界确定，避免导入后的模块依赖开发仓库路径。保持模块权限、命名 view 和预算边界，不涉及已有记录序号重写或 session 迁移。

**验证与验收标准**

- 多页结果按条数或字符预算截断时均能完整遍历，空结果正常结束。
- 长系列中上一篇选择及下一业务序号计算正确，不受旧的单页 500 条限制影响；验证实际排序使用业务序号。
- 各模块不同分页预算仍生效；只取最新条目的查询不扫描整个系列。
- 两条记忆目录生成路径使用同一字段转换，输出保持一致。

### R11：近场与广域模块共用机械处理实现

确认日期：2026-09-13。当前验收状态见顶部进度表及最终验收报告。

**处理目标与方案**

1. 保留近场、广域两个独立模块，抽出相同的故事包校验、创作资料准备、发布及近期故事导出函数，减少平行维护。
2. 通过模块配置提供 module ID、record type、文案和确有必要的业务差异，避免两份近似代码持续独立演变。
3. 共同故事字段结构保留一个定义来源，候选校验、导演审核后组装及持久化 schema 尽量复用；安装布局需要独立 schema 文件时由代码生成并验证一致性。
4. 保留各模块的 Agent、创作规则、文风、素材、权限、数据归属，以及近场 `scene`、广域 `topicId` 等业务语义独立。
5. 与 R10 共用分页及正确的序号查询机制；相同机械逻辑修复一次即可覆盖两个模块。

**影响范围与兼容**

根目录近场/广域模块的机械工作流代码、共用故事结构和相关导演审核组装代码、测试及导入说明。共用代码的位置和交付方式须满足模块复制进卡后可运行的安装边界，不能引用开发仓库中的路径，也不将模块业务规则硬编码进通用工作流引擎。保留独立导入能力，不自动同步已有卡片或迁移 session。

**验证与验收标准**

- 两模块相同的候选校验、发布和目录导出行为由共用实现覆盖，业务差异按各自配置生效。
- 两模块的权限、记录类型、数据归属及创作提示仍独立，无跨模块误读写。
- 共用字段约束在候选、审核组装和持久化各阶段保持一致；生成的 schema 无单边漂移。
- 分别检查单模块及同时导入时的依赖完整性，复制后的模块无需开发仓库路径即可加载共用实现。

### R12：统一生图执行与阶段恢复

确认日期：2026-09-13。当前验收状态见顶部进度表及最终验收报告。

**处理目标与方案**

1. 普通生图与 Web 重新生成共用请求/生成记录创建、ComfyUI 调用和状态更新流程，由各入口提供业务参数，不再分别维护相同的执行代码。
2. 保留提示词来源差异：普通生图按需调用提示词 Agent；直接重新生成继续使用已有或用户修改的提示词，不为复用执行流程而额外调用 Agent。
3. 按实际执行阶段与当前记录 revision 更新状态，保留版本冲突检查，消除错误处理使用固定旧 revision 的问题。
4. 区分 ComfyUI 生成失败与已生成但本地结果保存失败；恢复时优先补做未完成步骤，不重复生产已有提示词和已确认的生成结果。
5. 与 R04 统一请求身份和状态检查：同一次操作重复到达时复用任务，已完成则返回结果，失败则根据已有阶段进度恢复。
6. ComfyUI 是否已接收任务不确定时，先查询可用的任务状态，不能直接假定未提交并重新生成。实施时明确可用任务标识、查询能力及仍无法确认时的状态表达，不把不确定结果标成已成功或未提交。

**影响范围与兼容**

根目录 ComfyUI 模块执行代码、Web 重生成入口、共用执行服务及相应状态记录、接口说明和测试。保留两类入口的用户操作方式及原有数据权限。请求身份字段、历史状态兼容读取与 R04 一起确定；如需数据结构调整，明确版本和兼容策略，不自动迁移已有 session。

**验证与验收标准**

- 普通生图和重新生成进入同一确定性执行实现，按各自提示词来源生成正确请求。
- 同一请求重复到达不启动重复的 Agent 或 ComfyUI 任务；用户明确再次生成仍作为新操作处理。
- 已写入 submitted 后保存 completed 失败，恢复或错误记录使用正确 revision，不因写死版本号再次失败，也不覆盖其他并发修改。
- 覆盖生成失败、结果保存失败、任务已接收但响应不确定等情形，保留实际阶段信息并执行对应恢复策略。
- 已确认完成的结果可以补登记或直接返回，不因本地保存失败再次生成；使用模拟服务验证，不以真实付费生成作为默认测试。

### R13：为生图 Agent 生成专用资料文档

确认日期：2026-09-13。当前验收状态见顶部进度表及最终验收报告。

**处理目标与方案**

1. 由代码区分执行数据与 Agent 输入：连接、配置版本、文件摘要等执行信息供代码使用，不随完整执行对象传给提示词 Agent。
2. 生成 Agent 专用资料文档，首部统一说明参考内容、画面目标、模型指导的用途和输出格式；随后提供各模型指导及必要的 `guideId`、画面目标、用户额外要求、参考剧情和人物外貌等连续性资料。
3. 同一参考剧情、目标和额外要求在 Agent 输入中只出现一次，消除原顶层字段与中文 `promptInput` 的重复交付。执行代码需要保存或使用的值仍可从执行数据取得。
4. Agent 接收文档入口，不同时接收完整执行对象；采用文件交付时，按 R02 启用 Pi 自带的 `read`。
5. 保留生图所需的模型指导、人物资料和其他有效创作输入，与 R06 的非聊天资料保留要求一致；本项不更改提示词 Agent 的业务职责。

**影响范围与兼容**

根目录 ComfyUI 输入准备代码、生图工作流交付配置及 Agent 工具/提示配置、对应文档和测试。后续持久化与 R12 的共用执行服务继续取得完整所需执行数据。不涉及已有请求记录清理、图片处理或 session 迁移。

**验证与验收标准**

- 检查实际 Agent 输入及交付文档，参考剧情、画面目标、额外要求没有以两套字段或内联/文件两种方式重复展开。
- 资料文档首部有完整使用指导，模型指导、必要 `guideId` 和人物连续性资料完整可读。
- 连接 ID、配置摘要等纯执行字段不进入 Agent 专用输入，执行节点仍取得其所需数据。
- Agent 通过 Pi 的 `read` 读取资料并按约定输出，后续代码能按 `guideId` 正确使用提示词。

### R14：使用稳定来源身份避免交接重复捕获

确认日期：2026-09-13。当前验收状态见顶部进度表及最终验收报告。

**处理目标与方案**

1. 为导演归档交接生成稳定捕获 ID，依据来源模块、交接记录 ID 及需要区分的内容版本，而非子工作流运行 ID。
2. 同一份交接在捕获成功但导演侧确认失败后重试，复用原捕获 ID；记忆模块识别已经成功捕获的记录，复用结果，不再次保存相同来源版本。
3. 导演侧确认时直接使用确定的捕获 ID，不再根据子工作流运行 ID 推算 `sourceCaptureId`。
4. 来源内容真实修订时按版本规则形成新的交付，不能被同来源去重误丢弃；实施时区分内容版本与单纯捕获状态变化，并明确对应规则。
5. 仅由代码处理同一来源版本的重复发送；不同来源资料是否描述同一事件及应如何合并，继续由记忆 Agent 灵活判断，不收紧 R03 的维护语义控制。

**影响范围与兼容**

根目录导演 outbox 捕获/确认代码、记忆来源捕获的重复识别、相关接口说明和测试。与现有故事来源稳定捕获 ID 方式保持一致的重放语义。保留来源记录及 revision 校验，不自动清理历史重复记录或迁移已有 session。

**验证与验收标准**

- 模拟捕获成功、导演确认失败，再次执行后只存在一份相同来源版本的成功捕获记录，并能补齐导演侧确认。
- 子运行 ID 改变不影响同一份来源的捕获身份；`sourceCaptureId` 始终指向实际复用或创建的记录。
- 来源内容真实修订可重新交付，单纯技术重试不会创建新的内容版本。
- 不同来源记录不会仅因文本相似而被代码合并或丢弃。

### R15：统一有效配置并区分初始值与当前运行状态

确认日期：2026-09-13。当前验收状态见顶部进度表及最终验收报告。

**处理目标与方案**

1. 每次检索开始前，由代码读取已保存的默认设置，按调用授权处理本次覆盖值，解析出一份有效预算，同时交付检索 Agent 和结果组装代码使用，避免两者各自取常量或不同参数。
2. 接通“允许自定义检索预算”开关，明确默认值、覆盖值和现有授权上限的作用，避免设置仅存在于界面和记录中。
3. 区分压缩初始配置与当前状态：初始值用于初始化，当前值用于实际运行和成功压缩后的增长；界面明确展示当前值及初始值的用途。不通过修改初始值隐式重置当前进度。
4. 若提供当前阈值调整功能，使用明确的当前阈值操作，不复用语义含糊的初始值编辑；具体操作设计需明确后实施，不据本项自动重置当前状态。
5. 核查其他设置，接通仍需要的配置，移除或明确标注不再生效的配置；对尚不能确定业务意图的设置不擅自赋予新行为。
6. 本项先统一配置的实际生效与交付，暂不增加初次查看、补查条数等新的硬性查询计数限制。保留 Agent 的灵活处理以及现有权限、预算校验；不将审查报告中的硬计数建议自动实施。

**影响范围与兼容**

根目录 narrative-memory 设置读取、检索准备和组装、压缩初始化/当前状态展示、前端设置说明及测试。与 R07 的明确参数接口衔接。实施时明确设置缺失时的初始化或兼容行为，不保留多个互相竞争的有效配置来源。不自动迁移已有 session 或重置其压缩进度。

**验证与验收标准**

- 修改持久默认检索设置后，下一次检索的 Agent 输入与代码组装使用同一份有效值。
- 自定义预算开关和调用覆盖规则实际生效，符合现有授权边界。
- 压缩初始值、当前值及每次成功后的增长含义清楚；初始值编辑不隐式改写当前阈值或累计进度。
- 已展示为有效的设置均有对应消费逻辑或明确用途说明。
- 不增加本项已决定暂不采用的 Agent 查询次数硬限制。

### R16：集中交付资料使用指导并清理冲突文档

确认日期：2026-09-13。当前验收状态见顶部进度表及最终验收报告。

**处理目标与方案**

1. 每份交付文档首部提供内容说明和使用指导；多文件资料包统一在根 `DOCUMENTS.md` 首部说明所含资料、阅读方式、任务用途及必要权威性和限制。
2. Agent 和工作流提示保留职责、执行时机及阅读入口，不各自重复维护整套资料使用说明。
3. 参数名称、基本类型与调用示例和 R07 的接口定义一致；可由代码生成的机械签名等信息由代码生成，避免手工维护平行版本。
4. 开发和导入文档解释如何接入，清理过时流程，包括要求 Agent 自行打包导演输入的旧说明，以及与当前自然语言检索接口冲突的结构化请求示例。
5. 内容特有的知情范围、适用条件等仍就近标注；不将这类必要局部说明当作冗余删除。
6. 逐条保留有效规则的含义和可达性，确保迁移后的指导位于对应 Agent 实际可读的交付资料中；不能因减少提示重复而丢失规则或移至不可访问的开发文档。

**影响范围与兼容**

根目录各模块资料生成器、相关 Agent/工作流提示、模块 Skill 参考、IMPORT 与集成文档、对应检查和测试。与 R01 的阅读入口、R02 的工具可用性、R06 的必要资料保留及 R07/R15 的参数和配置规则衔接。不更改业务语义，不自动修改已有卡片或 session。

**验证与验收标准**

- 交付资料首部或资料包根索引提供清楚的说明与使用指导，接收者可先了解再阅读正文。
- 相同通用指导有明确维护位置，其他位置仅保留职责、时机或入口引用；必要局部规则保留。
- 调用说明、示例和实际接口一致，已知自动快照与手工打包、自然语言与旧结构化请求冲突消除。
- 对比迁移前后的有效规则，确认无遗漏、无含义变化，相关 Agent 实际可读取所需指导。

### R17：按节点需求选择交付资料与表示格式

确认日期：2026-09-13。当前验收状态见顶部进度表及最终验收报告。

**处理目标与方案**

1. 工作流内各节点明确声明所需输入文档，由代码选择、组装并交付，不默认向每个节点复制工作流收到的全部资料。
2. 近场发布节点只取得近场审核结果，广域发布节点只取得广域审核结果；提交节点按实际提交需求取得数据，不因同属一个工作流而附带全部创作参考资料。
3. Markdown 与 JSON 按消费者的实际用途生成和交付；只需要其中一种表示时不无条件附带另一种，两种均有必要时保留两种。
4. 选定资料的说明、阅读入口和内部相对引用保持完整，与 R01 的目录交付及 R16 的首部使用指导规则一致。
5. 需要完整上下文的 Agent 仍取得其任务所需的完整资料，不以减少复制为由任意缩减固定提示、非聊天资料或必要历史；与 R06 的显式资料范围衔接。
6. 优先消除不必要的复制与交付，不将共享存储、跨节点缓存或新的缓存失效机制作为本项实施前提。

**影响范围与兼容**

根目录公共运行时的节点资料准备、相关工作流输入声明、模块资料包筛选及导出格式、对应文档和测试。实施时核查现有消费者并同步补齐声明，保留路径边界、权限、冻结语义和资料生命周期，不自动修改已有卡片、session 或历史快照。

**验证与验收标准**

- 从实际工作流执行路径检查各节点收到的文件，未声明且无用途的整套调用资料不被自动复制。
- 近场与广域发布分别只取得各自所需结果，提交节点仍可完成提交。
- 仅需 Markdown 或 JSON 的消费者只取得所需表示；确实需要两者的消费者均能正常使用。
- 所选资料的入口、使用指导与相对路径有效，需要完整上下文的 Agent 所需资料无遗漏。
- 在不依赖新共享存储或跨节点缓存的条件下，减少可验证的无用资料复制。

### R18：由代码组装固定提交协议并处理确定性排序

确认日期：2026-09-13。当前验收状态见顶部进度表及最终验收报告。

**处理目标与方案**

1. Agent 输出需要判断的业务操作、修改内容及必要业务字段，不再承担固定提交协议的重复拼装。
2. 由共用代码填写固定协议版本、提交策略、批次状态及批次/操作技术 ID，统一现有各提交路径的机械组装逻辑，并同步调整 Agent 输出约定和消费端。
3. 仅对规则明确且不存在依赖冲突的操作执行确定性排序，例如经核实可安全前置的归档交接操作；存在操作间引用或依赖时保留其业务语义，不对所有操作盲目排序。
4. 保留模块/集合权限、数据结构、revision 和事务校验。不得为使提交成功而将预期版本自动替换为最新版本，也不得任意重写被其他操作引用的业务记录 ID。
5. 事实提取、事件与认知区分、语义压缩分组及导演判断继续由 Agent 完成；遵循 R03 的记忆维护灵活性决定，不增加严格维护范围控制，也不引入 R15 已暂缓的硬性查询计数限制。

**影响范围与兼容**

根目录导演前置、后置及审核相关 Agent 输出约定、批次提交与审核组装代码、核查发现的同类机械包装路径、对应文档和测试。共用实现须满足模块安装边界；同步调整生产与消费端，保留原有业务操作含义、数据权限及追踪能力。不自动修改已有卡片、session 或历史批次。

**验证与验收标准**

- Agent 只提供约定的业务操作和必要字段时，代码可生成合法完整的提交批次，固定协议值及技术 ID 无需模型重复填写。
- 各适用提交路径复用相同的机械组装规则，批次及操作仍可追踪。
- 已明确且无依赖冲突的操作可按规则排序；存在记录引用或顺序依赖的操作不因排序或 ID 改写而改变含义。
- 无效数据、越权访问及版本冲突仍被原有机制拒绝，不通过自动刷新预期版本绕过冲突。
- Agent 的业务判断职责和 R03/R15 已确认的保留项不受影响。
