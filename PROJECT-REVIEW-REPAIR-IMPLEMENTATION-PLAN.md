# 最终验收问题修复实施方案

日期：2026-09-13。状态：**代码已实施，但 2026-09-14 再次复核不通过；8 项缺陷/验收缺口尚待修复。** 详见 `PROJECT-REVIEW-RECHECK-2026-09-14.md`。

依据：[最终验收报告](M:/ai/SillyTavern/bobo-agent-rp/PROJECT-REVIEW-FINAL-ACCEPTANCE-2026-09-13.md)、[原实施方案](M:/ai/SillyTavern/bobo-agent-rp/PROJECT-REVIEW-IMPLEMENTATION-PLAN.md)。覆盖 V01—V14，以及发布完整性检查、独立资料准备并行两项缺口。原 R01—R18 的用户决定继续有效。

## 一、目标与边界

本轮目标是让已确认方案在实际组合路径中成立：入口能启动、必要资料确实到达、无需执行时正常结束、失败不会被报为成功、同次操作可恢复且不会盲目重复生产。

- 修改范围：根目录公共运行时/Web 模板、全局模块、相关技能参考、测试、发布检查和本方案记录。
- 不自动同步已有卡片、安装运行时或 `play/`，不修改、迁移或清理已有 session。新数据字段的兼容规则在下文明确；它们不构成已有 session 的迁移授权。
- R03：继续由记忆 Agent 灵活判断关联维护，不增加目标记录硬白名单、严格语义范围控制或关联修改二次确认。
- R15：统一实际有效预算，但不增加初次查看、补查等新的 Agent 查询次数硬限制。
- 保留 Pi 系统提示、模型首尾提示、Agent 职责提示、节点任务提示、节点原有的固定上下文配置，以及任务需要的静态规则和人物资料。
- 权限、expectedRevision、事务、路径边界及必需输出校验保持有效；不以放宽校验、刷新预期版本或生成空资料掩盖失败。
- 不自动提交 Git、发布或调用真实模型/ComfyUI 生成。确定性验收使用模拟服务，但运行实际工作流、调度器、资料交接和提交路径。

## 二、实施顺序与交付

| 批次 | 修复项 | 依赖与交付要求 |
|---|---|---|
| A：执行正确性 | V01、V04、V13 | 先建立可复用的真实工作流测试装配，再修正身份、触发和提交失败传播 |
| B：资料链路 | V03、V02、V08、V09、V12 | 先调整压缩节点职责，再一次性补齐文档生成、选择和消费者声明；避免中间阶段继续丢输入 |
| C：生图生命周期 | V05、V06 | 依赖 A 的身份/失败语义、B 的 Agent 输入机制；两项一起交付 |
| D：数据与共用实现 | V07、V10、V11、V14 | V07 的索引修复先于故事共用实现终验；V14 依赖 V13 的确认失败传播 |
| E：剩余缺口与总验收 | G01、G02、全量复验 | G02 在 B 的资料依赖明确后实施；G01 检查全部新增生成资产和模块文件 |

每批均记录改动、回归结果和剩余问题。只有真实链路的对应验收通过，才关闭相关 V 项；原 R 项须在它关联的全部缺陷关闭后才能标记通过。

## 三、逐项修复设计

### V01：规范工作流身份，消除同名实例冲突

**处理方式**

1. 公共运行时集中计算工作流身份：顶层使用 `top-level/<id>`，模块使用包含 ownerModuleId 的模块身份。所有模块引用统一经过同一函数，不在调度器各处分散拼接。
2. 实例计数、实例键、模块调用等待和恢复登记使用规范身份。按实例参数区分多实例任务，按现有约定保留模块内部串行与集合写锁。
3. 恢复时从保存的工作流定义、ownerModuleId 和原实例参数重建相同键，不继续使用与正常启动规则不同的临时 restored 键。
4. 审查涉及工作流身份的调度记录与缓存；仅迁移实现所需的内存键，不更名业务工作流，不改写已有运行记录。

**范围**：`rp-workflows.mjs`、`rp-workflow-engine.mjs`、对应调度测试及工作流协议说明。

**验收**：真实同名生图/记忆顶层入口与子模块可以执行；两个不同模块使用相同 workflow ID 不相互占用实例；同一规范工作流的实例上限仍有效；恢复后同键任务不能重复启动；不通过改测试中的业务 ID 规避问题。

### V04：保留模块调用的业务触发来源

**处理方式**

1. 子调用保留当前父流程的触发来源，逐层传递；调用父子关系仍放在 callContext，不用 `manual` 充当模块调用默认值。
2. 自动事件新启动的顶层任务以实际事件为触发来源。例如用户手动运行某流程后自动触发归档，归档来源仍是 `after-workflow`，不能因更早的用户操作变成手动归档。
3. 手动归档/压缩入口只在用户明确启动相应任务时携带手动语义。核对允许手动触发、保护范围、冷却和压缩阈值的实际消费代码。
4. 来源缺失的模块调用不能默认取得手动例外：新调用必须有来源；旧运行记录如无法确定来源，按无手动例外处理并保留可诊断原因。

**范围**：工作流创建、模块调用、恢复以及记忆任务准备代码；相关调用说明。

**验收**：自动/手动入口经过一层及多层调用保持正确含义；自动任务不绕过阈值和冷却；允许的手动任务仍能按既有规则执行。

### V13：统一代码提交的失败传播

**处理方式**

1. 公共数据层提供统一的回执判定辅助函数，明确正常 committed、已提交幂等重放、failed、partial 和未知状态的处理。合法幂等成功以实际回执协议为准，不假设返回结构。
2. 可信代码节点的 `data.submit` 使用严格提交封装：非成功回执抛出包含批次 ID、错误码及回执的异常。保留底层返回完整回执的 API，供明确需要处理非原子结果的调用者使用；不全局改变 Agent 查询或已有底层事务 API。
3. 直接调用底层服务的 Web 路径也接入同一判定。核查导演提交、审核副作用、故事发布、记忆提交、生图状态及捕获确认，消除“等待了 submit 就认为成功”的写法。
4. 模块测试使用真实回执或符合真实协议的模拟回执；不保留以 `undefined` 代表成功的模拟提交。
5. failed/partial 不放行正常成功下游，不写 committed:true，不推进归档进度、不确认 outbox、不发布审核结果。需要重试时沿用已有事务重放规则；不得自动替换 expectedRevision。

**范围**：公共数据提交辅助能力、代码节点 facade、Web 直接提交路径及相关模块调用者。

**验收**：真实数据服务返回版本冲突、权限拒绝、事务失败时，节点和工作流均呈现失败；受影响的成功下游不启动；幂等成功可正常继续；明确授权的部分提交不会被误报为全量成功。

### V03：拆分压缩资格判断与有条件资料生成

**处理方式**

1. `prepare-compression` 只负责启用/阈值/手动条件判断，并在需要判断阈值时读取一次有效事件集合；不声明一个跳过分支不可能生成的必需快照。
2. 新增受 `compress` 路由控制的确定性资料节点，读取上一步已计算的有效目录投影，生成时间线和压缩任务文档；该节点不再查询事件。
3. 已读取事件只保留下游确实需要的目录字段、来源 ID/revision 和阈值资料作为机器结果，不把完整维护对象转交 Agent。复用原时间线转换函数。
4. 规划 Agent 等待资料节点；提交节点继续使用准备时的状态 revision，并做已有的提交时有效性检查。
5. skip 路由跳过资料节点与所有模型节点，正常返回业务跳过结果。保持必需 workspaceHandoff 校验，不为此引入全局“缺文件也成功”。

**范围**：压缩工作流、资格准备/资料生成代码、完成钩子集成测试。

**验收**：关闭时不查事件、不产资料、不调用 Agent；低于阈值时只做必要资格读取；满足条件时资格计算与资料共用一次事件读取；三条路径均经过真实 `finalizeNodeData`，无必需输出缺失。

### V02：按消费者补齐必要资料

**处理方式**

采用显式、经过代码裁剪的资料文档作为默认交付方式。`context.fromNodes` 只表示依赖和来源，不能被当作内容已交付的证明；不恢复向所有 Agent 展开原始运行对象。

| 消费节点 | 必需交付内容 | 交付方式 |
|---|---|---|
| 归档一级 | 指定范围剧情、早期参考、选定模块来源、对应归档指导 | 代码生成归档任务资料包；区分参考与待归档内容 |
| 归档二级 | 上述原始资料、完整必要目录、一级清单、相关超限摘要任务 | 原资料包按需继承；一级清单声明独立 Markdown 输出 |
| 范围修复一级/二级 | 明确范围内剧情与来源；二级另需一级清单 | 同归档共用机械资料组装，保留范围修复业务规则 |
| 维护 Agent | 本次维护请求、准备好的目标记录及其 revision、所需目录 | 独立任务资料；不限制 Agent 的关联语义判断 |
| 检索 Agent | 自然语言查询、可选补充文档、解析后的有效预算、完整目录 | 查询选定一个正文载体；预算由准备节点生成明确资料 |
| 压缩 Agent | 有效事件时间线、当前目标条数及既定规划规则 | 使用 V03 的资料包 |

1. 多文件包的根 `DOCUMENTS.md` 先解释用途、参考/事实边界、阅读顺序和必要限制。技术提交状态、完整 settingsRecord、无用 envelope 不交给 Agent。
2. 原始模型清单需要下传时声明为真实输出，由公共完成路径保存/登记，避免再让 Agent 手工复制或重新打包。
3. 逐节点补齐 inputs、outputs、workspaceHandoff、callInputs 及必要文档元数据。只有确实需要原始上游值时才使用 nodeOutputInputs，并给出用途。
4. Agent 与最终组装代码使用同一次解析出的 effectiveBudget。调用覆盖关闭时，Agent 不再看到可能与实际有效值冲突的未采用预算作为执行规则。
5. 核对已有职责提示与资料首部的规则分工，保留语义规则，删除过时的“Agent 自行决定是否跳过”要求。

**范围**：记忆归档/范围修复/维护/检索/压缩的生产节点、Agent 节点及相关提示与参考说明。

**验收**：真实代码准备、输出登记、交接和最终提示装配中，逐条断言必需内容可读；未选定历史和无用执行字段缺席；一级清单确实到达二级；改持久预算后 Agent 和组装器取得同一有效值。

### V08：选定回合只准备一次，并显式冻结给需要的导演

**处理方式**

1. 公共准备代码按前台工作流的 recentCompleteTurns 选取完整回合，生成已注册的聊天资料文档，记录确切消息 ID/revision 和可见回合上界。
2. 文档工作区前台 Agent 从该资料读取最近回合，取消同一段正文的额外内联。当前玩家输入继续只保留一个直接输入载体；冻结给下游时由代码建立相应文档。
3. 前置自动快照和正文结束快照包含所需的已注册最近回合资料；两者都保持目录入口、相对引用、来源信息和现有大小限制。
4. 深度导演增加明确的剧情文档输入，并逐一接通手动、开场、回合后及自动深推包装入口。已有前台快照可用时复用其中选定资料；没有快照时由可信代码按调用方明确的回合范围准备，禁止退回默认全历史。
5. 深度导演窗口来自调用工作流配置；与前台联动的调用默认沿用其完整回合范围。特殊任务要全历史时须显式配置，作为同一文档交付机制的范围选项。
6. 后台任务开始时固定所选正文和来源 revision，延迟执行不重新从活动对话拼接新的版本。恢复需要的冻结资料必须保留到消费者完成；跨回合消费者不能依赖下一轮即清理的临时文件。
7. 非文档工作区的兼容路径继续按明确模式交付历史，确保一个节点不会同时得到等价内联与文档副本。

**范围**：前台模板、公共回合资料准备和快照、深度导演接口、全部深推包装入口及生命周期说明。

**验收**：正文、前置、后置和深度导演取得约定范围内的完整回合；未选回合不进入；后台启动后新增/修改聊天不改变已冻内容；消费者延迟跨回合运行时仍可读；固定提示及必要非聊天资料前后对比无遗漏。

### V09：Agent 参数按节点选择，去除生图 request 重复交付

**处理方式**

1. 公共 Agent 节点增加明确的参数选择声明，例如 `metadata.argumentInputs`。默认不把全部 run.arguments 暴露给 Agent；声明只能引用接口内实际存在的参数。
2. 基本参数仅在 `CALL-INPUTS.md` 写入一次；动态提示只给阅读入口和任务，不重复 JSON.stringify。文档索引仍记录所需路径。
3. 大参数或包含技术字段的对象先由代码生成裁剪资料，不增加任意路径表达式来代替必要的业务组装。生图提示 Agent 不声明 request 参数，仅读取 prompt-context。
4. 审核所有模块 Agent 的真实参数需求，包括故事 assignment、维护请求和深度导演 triggerReasons；在切换默认行为的同一批次补齐声明，防止重现 V02。
5. textInput 作为任务消息交付时，索引只说明其存在；已整理入专用文档的同一文本不再另行内联。执行代码继续从 run.arguments 取得完整必要执行对象。

**范围**：工作流声明校验、调用输入索引、动态提示组装、各模块 Agent 输入声明与生图资料准备。

**验收**：实际 prompt-context、CALL-INPUTS 和最终提示合并核对，生图目标、额外要求只出现一次；连接/配置摘要等执行字段不进入 Agent 输入；故事 assignment 等必要参数没有因默认裁剪而消失。

### V12：触发资料和审核结果也按节点选择

**处理方式**

1. 为触发资料增加显式节点选择声明，例如 `metadata.triggerInputs`；未声明的触发文档不复制。校验声明必须对应工作流触发输入。
2. 上游产物增加基于“生产节点 ID + 输出 ID”的明确选择能力；需要限定输入的节点按此选择，不能仅靠 fromNodes 复制该节点全部输出。协议兼容已有声明时也必须把本轮相关模板迁移到显式集合。
3. 审核生产者按 local/world 分别声明交付输出，分别保留审核状态及该频道所需包；publish-local 仅继承 local，publish-world 仅继承 world。
4. 未委托、被暂缓、没有候选等分支使用明确业务状态和路由；条件性包不强制声明成所有分支都必须存在的文件，复用 V03 的职责拆分思路。
5. 提交节点只接收提交机器结果；需要进行来源捕获的发布节点保留捕获所需来源与元数据，不附带完整前台资料。

**范围**：公共触发/上游交接、工作流协议、导演审核导出、导演叙事总流程与发布包装。

**验收**：检查各实际节点目录与复制清单；两个发布节点无对方故事、无无用冻结历史；无候选和 withheld 分支正常结束；选定文档入口和相对路径完整。

### V05、V06：统一生图操作身份、提示词复用和提交恢复

两项作为一个生命周期改动实施，避免前置去重与后端恢复采用不同状态。

**操作入口与提示词阶段**

1. 用户一次生成动作建立 operationId，HTTP 重发和内部重试沿用；用户明确再次生成建立新 ID。对缺失/非法 ID 的现有入口明确处理规则，不能在重发过程中悄悄换 ID。
2. 普通生成、模块调用和 Web 重生成使用同一操作服务。按 session + operationId 查已有请求/运行，并发重复请求返回同一运行或已有结果。
3. 在 Agent 前建立请求身份并冻结输入。请求结构增加提示词准备阶段及对应运行关联；只有提示词未完成时才允许为空，完成阶段仍要求每个必要 guideId 有合法提示词。渲染总体状态从 render 记录汇总，避免再维护一套重复的总状态。
4. 已有提示词和渲染记录优先复用；普通生成只在未取得持久化成功提示词时调用 Agent，直接重生成继续使用已有或用户修改的提示词。
5. 同 operationId 携带不同业务输入返回明确冲突，不能覆盖原操作；该比较仅保护同 ID 重放，不按内容阻止新的操作。
6. 提示词结果在放行渲染前保存。若模型返回但保存尚未成功便崩溃，只保证复用已可靠保存的结果，不承诺对无法恢复的模型响应做到绝对只调用一次。

**渲染提交与恢复**

1. 为 render 增加 `submitting` 阶段和提交尝试身份。在发出远端请求之前，用 expectedRevision 将 pending 转为 submitting，并保存所用连接、可核对的客户端任务身份及必要执行配置。
2. 并发执行者只有成功取得阶段转换者可以发起该次提交。恢复遇到 submitting/submitted 时先检查已有任务；不能当作 pending 再 POST。
3. 实施前核对实际适配 ComfyUI 对客户端任务 ID、历史及队列查询的支持，优先查已接入服务代码/契约，必要时核实官方实现。若可由客户端指定并可靠查询任务 ID，先持久化该 ID 再提交；若不能，保存可支持的关联标识，无法确认的任务保持不确定，不能凭空承诺远端 exactly-once。
4. 收到确认后转 submitted；收到生成结果后写 completed。响应丢失、JSON 解析失败、本地 submitted/completed 登记失败分别保留对应阶段，查询原任务后补登记。
5. 仅在能够确认未接收或明确执行失败时允许再次提交；新提交尝试保存新的尝试身份，前次信息由已有记录修订历史保留。一次查询不到历史不等于从未接收。
6. 查询和恢复绑定原连接及已冻结的输出映射/执行配置，不因用户后续修改 profile 而去另一服务器查询或误认结果。已经保存的产物引用直接复用。
7. 无法自动消除的不确定状态在界面明确展示，提供查看原任务状态的方式；任何明确再次生成都作为新的用户操作，不伪装成原请求的安全自动重试。
8. 复用公共持久数据服务、事务及阶段检查，不仅依赖模块内的内存 Map；动态 import、重启或不同 Web 入口不能绕过去重。

**范围**：image.request/render schema、契约/view/capability、阶段处理器、统一执行服务、公共 ComfyUI 适配、普通/重生成入口、相关 Web 展示与文档。

**兼容**：新请求使用新阶段；旧请求有合法已存提示词时按“提示词已就绪”解释。旧 completed/submitted 记录按原身份读取；无法重建提交身份的旧 pending/failed 记录不得被误断定为安全未提交。只实现读取和显式操作的兼容，不批量改写 session。

**验收矩阵**

| 情形 | 预期 |
|---|---|
| 同 ID 并发到达或 HTTP 重发 | 复用同运行；不额外调用 Agent/POST |
| 已完成请求再次到达 | 返回旧结果；Agent 和生成调用计数不增加 |
| 内容相同的新 ID | 允许新生成 |
| 提示词已保存，渲染前中断 | 复用提示词继续 |
| submitting 持久化失败 | 不发远端 POST |
| 服务器接收后响应丢失/解析失败 | 保留不确定阶段，核对原任务；不盲目重新入队 |
| submitted 写入失败 | 根据预先保存身份恢复，远端入队总数仍为 1 |
| completed 写入失败 | 补保存已有结果，使用正确 revision |
| 明确拒绝或执行失败 | 允许约定的恢复路径，记录新的提交尝试 |
| 窗口重启/profile 被编辑 | 原操作身份、连接和已保存结果仍一致 |

### V07：业务序号使用独立索引名称

**处理方式**

1. 为故事 `/data/sequence` 增加不与 envelope 保留字段冲突的索引名 `storySequence`。上一篇、最新系列条目及下一个业务序号查询明确使用它。
2. 保留公共 `sequence` 排序对 envelope 的现有语义，避免破坏分页、日志及其他模块。兼容需要时可保留原索引作筛选别名，但不得继续把它用作故事排序。
3. 两个模块配置和共用故事助手一起调整；全目录仍完整分页，取上一篇仍 limit:1，不为修复排序而把所有查询改成全量读取。
4. 数据字段和已有故事序号不变；只新增可重建派生索引。新安装正确生成索引，不自动重建已有 session。

**范围**：近场/广域契约、共用数据助手和所有相关排序查询、实际公共排序测试。

**验收**：业务 701/envelope 1 与业务 700/envelope 2 的真实查询返回 701，下一序号为 702；覆盖超过 500 条、交错系列和导入顺序不同的场景；原 envelope 分页排序无回归。

### V10：按请求选择查询路径

**处理方式**

1. 快照开始时先解析请求，再决定查询计划。完整目录读取全部授权集合；仅时间线读取事件集合；二者组合复用同一次事件读取。
2. 精确记录 ID 通过受控 get 或已有 ID 索引取得；有 recordType 时缩小所属集合。无法唯一确定集合时只在允许集合中定位这些 ID，不先读取全部记录。
3. entityMode:entry-only 只取指定实体；related-records 直接调用已有关系索引路径，再按请求的 collection、record type 和剩余过滤条件裁剪。
4. 能安全下推的已有索引过滤提前执行，其他过滤保留原转换后的语义。时间线的摘要覆盖关系在完整所需事件集合上计算后再筛选。
5. 复用统一分页及 catalogEntry，不增加跨运行缓存；同次请求以 ID 去重并复用已取得数据。

**范围**：记忆快照请求解析、查询辅助与目录转换消费代码。

**验收**：记录实际数据调用；单实体关联请求无五类目录预读；精确 ID 不扫描全目录；完整/筛选/组合请求输出语义一致；空结果、分页和嵌套摘要覆盖正常。

### V11：故事字段、校验和 schema 使用同一规范源

**处理方式**

1. 在现有故事机械规范资产旁建立共同字段定义，描述故事元数据与持久化共有字段、基础约束及必要语义校验。
2. 候选校验与导演审核组装调用从该源生成的同一校验实现；持久化 schema 从该字段定义生成，持久化专用字段在同一规范源组合。
3. 两故事模块各带所需运行库/schema，导演模块只带审核所需的校验副本；安装后均从模块内部导入，避免跨模块或开发仓库路径依赖。
4. 扩展同步脚本及 --check，覆盖运行库、校验副本、schema；维护说明指向唯一规范源。
5. 保留 moduleId、recordType、scene/topicId、权限和创作规则独立。schema 能表达的约束及语义校验分工明确，同一个约束不再手工维护多个平行版本。

**范围**：共同故事资产、生成脚本、近场/广域故事库/schema、导演审核组装与相关测试。

**验收**：同一合法/非法 metadata 在候选、审核、持久化各阶段一致；替换故事亦校验；任一生成副本被改动可被检查发现；分别复制单模块及组合模块后可加载，无开发路径依赖。

### V14：以确定性的业务内容版本生成捕获 ID

**处理方式**

1. 在导演 archive-handoff 数据契约中正式增加代码维护的 `contentVersion`。不让 Agent 填写或决定该值。
2. 明确业务内容投影：recordKind、intent、subject、content、knowledgeScope、sourceTurn、dedupeKey。status、sourceCaptureId、envelope revision、时间戳等技术字段不参与内容版本判断。
3. 新记录由代码初始化版本 1；业务内容变化时在受 revision 保护的写入中递增；仅捕获确认、重试或纯状态变化保持版本。内容从 A→B→A 也产生新的版本，不仅按全文 hash 将第三次变化并回第一次。
4. 使用模块所有的确定性更新/确认处理器或统一的受控提交准备路径实现比较与版本生成；所有 outbox 写入入口都接入，不能只修改捕获读取端。若使用自定义动作，同步声明精确动作权限，Agent 仍输出业务操作，由代码补技术处理。
5. 旧记录没有字段时，以首次受控处理前的实际 revision 作为兼容基线；第一次受控更新保存基线或递增版本。捕获端和确认端使用同一计算函数，确认时保留刚使用的身份。既有 captured 记录不自动重新捕获。
6. 捕获 ID 由来源模块、来源记录 ID 和 contentVersion 组成；确认直接使用该 ID。确认提交失败由 V13 向上传播，重试复用捕获结果。

**范围**：导演 outbox schema/契约、全部创建/更新/确认入口、模块处理器、捕获桥接与真实契约测试。

**验收**：合法真实记录而非虚构字段的 mock；同版本确认失败重放仅一份捕获；状态修改身份不变；业务修改及 A→B→A 身份变化；同名/相似内容的不同来源不合并；legacy 无版本记录的基线稳定。

### G01：补发布完整性检查（R05）

1. 建立与项目模块声明同源的发布清单，避免 README、目录白名单和脚本各自维护互不核对的模块列表；检查模块清单及其运行时/schema/工作流/静态资料/生成副本的必要文件。
2. 提供两种明确检查目标：工作区可发布性检查使用实际文件和 Git 忽略规则，允许尚未暂存文件；发布检查使用 Git 暂存区或指定提交中的真实文件集合。未提交本身不等于缺模块。
3. 校验所有声明依赖文件都在对应目标中，而不只检查 module.json。核对现有个人设置、卡片和会话忽略规则未被放开。
4. 接入开发验收命令和发布说明，不自动添加、提交或发布文件。

**验收**：正常清单通过；缺整个模块、漏 runtime/schema、生成副本不一致、声明文件被忽略均失败；工作区模式不把正常未暂存文件误判为不存在。

### G02：并行独立的导演资料准备（R09）

1. 核查前置、后置和深度导演的私有资料、作者静态资料准备所读数据、写锁及输出依赖；记录确有串行必要的例外。
2. 对无依赖的两个准备节点移除互相串行依赖，让消费者显式 dependsOn 两者，保留同一可见回合和冻结范围。
3. 深度导演的 mark-running 等前置状态步骤保持必要顺序；资料可共同依赖该步骤，不能以并行为由提前启动有前置条件的工作。

**验收**：真实 DAG 加可控阻塞执行器验证两个独立准备同时就绪；任一未完成时消费者不启动；准备失败不产生缺资料的成功执行；原有写锁及来源边界不变。

## 四、兼容与失败恢复要求

- 工作流输入声明、生产者和消费者在同一批次更新，协议参考及 Python/JavaScript 校验同步。新增默认裁剪规则不能只改公共执行器。
- 新图像阶段、提示词准备状态及 outbox 内容版本须同步契约、schema、初始记录（如适用）、view、capability、处理器和前端显示；明确读兼容，但不自动升级已有会话。
- 生成源和可独立安装副本作为同一次变更交付；--check 只验不改，生成命令才更新副本。
- 每批使用隔离临时目录验证数据与恢复；失败时保留问题及原回执，不对用户工作区执行 reset、批量覆盖或清理。
- 状态不确定与确定失败必须区分。外部服务能力不足以消除不确定性时，记录实际限制；不能把“未找到”视为“肯定没执行”。

## 五、最终验收方法

### 必须接入的真实路径

1. **工作流执行**：使用实际顶层/模块 JSON，经规范化、调用授权、调度器、实例限制与恢复路径运行；不改名绕开冲突。
2. **资料交接**：运行真实生产代码、registerNodeArtifacts、finalizeNodeData、触发/调用/上游交接、快照及最终提示组装。对每个消费者断言必要文件、正文与来源，不能只断言配置字段存在。
3. **数据行为**：通过真实 RpDataStore、契约、索引/view、事务与回执验证排序、内容版本、冲突、权限及幂等重放。
4. **生图故障**：模拟 ComfyUI HTTP/历史/队列及模型结果，在远端接收、响应解析、本地阶段保存、结果登记等边界注入故障；统计实际模型调用、POST 次数和产物引用。
5. **安装及发布**：隔离复制模块后运行；检查规范源与生成副本、工作区/暂存区发布清单，确认没有开发仓库依赖。

公共执行器中目前难以测试的确定性装配逻辑可提取为公共函数，但 Web 本身必须调用这些函数；不另写一套只在测试中使用的装配逻辑。增加至少一条使用实际默认入口贯通这些函数的组合测试。

### 结束标准

- V01—V14 和 G01/G02 均有对应的通过证据；原验收探针能按预期行为通过。
- 全量现有 JavaScript/Python 测试以及新增集成测试通过，实际记录命令、可执行文件、退出码和测试计数。使用明确的 Python 路径；不以一串命令最后的退出码代替各项结果。
- 必要语法/类型检查、生成副本检查、发布检查、`git diff --check` 通过；检查不依赖真实付费服务。
- 原 R01—R18 逐项重新对照；R03 按保留决定关闭，R15 的不增加硬计数限制继续满足。
- 最终报告区分已验证行为、未运行的真实模型/真实服务边界、仍有问题的条目。存在确定性失败或未完成验收交付时，不标“全部完成”。

## 六、实施跟踪

| 项目 | 状态 | 完成证据 |
|---|---|---|
| V01/V04/V13 | 部分通过 | 已验证部分仍有效；F01 生图吞掉提交失败、F06 内部多实例键未接通，待修复 |
| V03/V02/V08/V09/V12 | 部分通过 | 已有任务物化/参数/交接测试有效；F07 后台排队跨回合丢失冻结剧情，待修复 |
| V05/V06 | 不通过 | F01—F05：失败传播、HTTP 不确定性、有效配置冻结、实际恢复入口及重放冲突仍有缺口 |
| V07/V10/V11/V14 | 已完成 | 业务序号排序、目标查询、故事规范同步、归档内容版本及重放测试通过 |
| G01/G02 | 部分通过 | G02 已有证据保留；G01 的实际 Git 目标内容校验缺口见 F08 |
| 原 R01—R18 总复验 | 不通过 | 上次总通过结论撤回，以 `PROJECT-REVIEW-RECHECK-2026-09-14.md` 的当前结果为准 |
