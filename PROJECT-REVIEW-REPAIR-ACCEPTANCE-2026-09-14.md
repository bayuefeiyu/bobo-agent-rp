# 项目审查修复最终验收报告

日期：2026-09-14。依据：[修复实施方案](M:/ai/SillyTavern/bobo-agent-rp/PROJECT-REVIEW-REPAIR-IMPLEMENTATION-PLAN.md)及[修复前验收报告](M:/ai/SillyTavern/bobo-agent-rp/PROJECT-REVIEW-FINAL-ACCEPTANCE-2026-09-13.md)。

## 结论

**本报告原“全部通过”结论已撤回。** 同日再次复核确认 8 项确定性缺陷/验收缺口，当前结论为不通过，见[再次复核报告](M:/ai/SillyTavern/bobo-agent-rp/PROJECT-REVIEW-RECHECK-2026-09-14.md)。下文原测试记录及逐项判定保留为历史记录，不能作为当前全部通过的依据。R03 保留 Agent 灵活控制、R15 不新增硬查询次数限制的用户决定继续有效。

本结论覆盖根目录开发源、公共运行时与 Web 模板、五个全局模块、技能参考和发布检查。未修改或同步 `play/`、已安装卡、现有 session，也未调用真实模型或真实 ComfyUI。真实端到端边界单列在报告末尾。

## 复验结果

- `node --test`：根目录全局模块与组合回归 `39/39` 通过。
- 公共运行时、Web 和 ComfyUI 适配测试：`122/122` 通过。
- Python 提取、技能契约、卡包校验和清单测试：`18/18` 通过。
- 叙事记忆 Module v6 包结构校验通过。
- 41 个实际顶层及模块工作流定义全部通过 Workflow v3 规范化。
- 121 个 MJS 文件及 Web `public/app.js` 通过语法检查；`pi-rp-web.ts` 通过 Pi 离线扩展加载。
- 故事规范源与生成副本同步检查通过。
- 发布清单工作区检查通过：5 个模块、145 个声明引用路径、224 个模块文件。
- `git diff --check` 通过。

## 修复缺陷验收

| 条目 | 结果 | 验收证据 |
|---|---|---|
| V01 | 通过 | 顶层与模块工作流使用带所属域的规范身份；同名工作流实例限制和恢复互不串扰 |
| V02 | 通过 | eligibility 准备不再提前写占位状态；任务物化器生成实际 Agent 文档和明确跳过路由 |
| V03 | 通过 | archive/range 的确定性检查结果以 Markdown 输出交给编辑 Agent，职责和输出路径一致 |
| V04 | 通过 | 父工作流的真实 trigger 传入模块调用；需要触发上下文的节点可取得同一冻结输入 |
| V05 | 通过 | 生图操作在 Agent 前持久化；同 operationId 重放复用已有请求，不重复调用 Agent |
| V06 | 通过 | render 在 POST 前写入 `submitting` 及稳定任务身份；响应丢失和完成写入失败均查询并恢复原任务，远端 POST 计数保持 1 |
| V07 | 通过 | 业务 `/data/sequence` 使用 `storySequence` 索引；真实公共排序器验证业务 701 不受 envelope 插入顺序影响 |
| V08 | 通过 | 选定完整回合生成注册文档并冻结给前台及所需导演；固定提示和必要非聊天上下文保持原有装配 |
| V09 | 通过 | Agent 只接收节点声明的参数；调用输入集中于 `CALL-INPUTS.md`，生图技术请求不再重复内联 |
| V10 | 通过 | 精确 ID 使用受控 get，实体 entry-only 使用定点读取，related-records 直接使用已有关系索引 |
| V11 | 通过 | 故事字段、schema 和语义校验由共同规范源生成；候选、审核、持久化及独立模块副本一致性受同步检查保护 |
| V12 | 通过 | 触发文档和上游 handoff 按节点选择；local/world 审核输出拆分，发布节点只继承自己的频道包 |
| V13 | 通过 | 公共严格提交辅助函数只接受 committed 回执；failed、partial 和未知状态向上抛错，不再放行成功下游 |
| V14 | 通过 | archive-handoff 增加代码维护的 `contentVersion`；业务内容每次变化递增，纯状态变化保持，同版本重放沿用稳定捕获 ID |
| G01 | 通过 | 发布清单与检查器核对模块目录、声明引用、运行时/schema/工作流/资源/生成副本及 Git 忽略规则 |
| G02 | 通过 | 前置、后置和深度导演的私有资料与作者静态资料独立准备；实际 DAG 验证消费者等待双方 |

## 原 R01—R18 复核

| 原问题 | 结果 | 处理归属 |
|---|---|---|
| R01 | 通过 | V01、V08：目录交接、入口调用、身份和冻结资料链路 |
| R02 | 通过 | read 工具配置与文档工作区运行前校验保持有效 |
| R03 | 按决定保留 | 记忆模块继续由 Agent 灵活把控，不加入严格语义目标控制 |
| R04 | 通过 | V05、V06：操作身份、阶段持久化及安全恢复 |
| R05 | 通过 | G01：发布完整性清单和自动检查 |
| R06 | 通过 | V02、V08、V09：必要历史唯一交付、参数裁剪和任务物化 |
| R07 | 通过 | V01：模块调用身份、接口和实际同名组合回归 |
| R08 | 通过 | V10：按请求选择定点、索引或完整查询路径 |
| R09 | 通过 | V03、V04、G02：Agent/代码职责、触发交接和独立准备并行 |
| R10 | 通过 | V07：业务排序与 envelope 排序分离 |
| R11 | 通过 | V11：共同故事结构、校验和生成副本机制 |
| R12 | 通过 | V05、V06：外部副作用的不确定状态和恢复边界 |
| R13 | 通过 | V09：调用数据仅按节点需要传递 |
| R14 | 通过 | V14：确定性内容版本与捕获身份 |
| R15 | 通过 | V02：有效预算由代码计算并共同交付 Agent/组装器；未增加硬查询次数限制 |
| R16 | 通过 | V02、V03、V09：入口说明集中于任务文档首部，技术字段不交给 Agent 重写 |
| R17 | 通过 | V02、V08、V12：必要资料保留，无关触发及跨频道资料裁剪 |
| R18 | 通过 | V13：统一提交回执判断，失败不会伪装为 committed |

## 验收中发现并修复的问题

全目录规范化首次发现生图内部工作流配置为 keyed multiple，而运行时仍一律拒绝 `module-internal` 多实例。运行时规则现允许使用精确集合写锁的 keyed multi-instance 内部工作流；默认整模块锁仍只允许单实例，实际集合锁调度仍会串行化相互冲突的实例。新增正反测试后，41 个工作流定义全部通过规范化。

Python 首次组合调用因含连字符的路径被 `unittest -m` 当成无效模块名而没有执行。该结果不计入测试失败；改为逐文件调用后实际执行 18 项，全部通过。

## 尚未执行的外部验收

- 未重新转换一张样板卡并在 `play/` 中完成真实 RP 回合。
- 未启动真实模型检查最终提示内容、模型产出和长会话行为。
- 未连接用户实际 ComfyUI 队列验证具体工作流、模型文件、history 与图片代理。
- 未对已有卡或 session 做迁移；本轮兼容代码只提供读取和显式操作边界。

这些项目需要真实卡片、模型或用户设备服务，不能由当前确定性测试替代；它们不改变本轮已确认代码修复范围的通过结论。
