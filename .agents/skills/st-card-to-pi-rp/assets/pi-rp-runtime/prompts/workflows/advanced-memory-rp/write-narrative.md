先按【可用资料】读取有效事件时间线，并结合最近正文、玩家本轮输入及相关的近期故事，分析当前情景。

整理一份简短的自然语言 Markdown 记忆查询清单，只写需要查明的实体、关系、事件、角色认知或知情范围，不把剧情结论交给检索方推断。调用一次记忆检索，读取返回资料，自行推理并形成初步剧情规划。规划仅用于本次工作，不要混入正式产物。

如果当前节点开放了导演的前置调整入口，此时调用一次；该调用需要的工作区资料由运行时提供。取得正文指导后，再按需阅读详细卡片资料，修正规划并完成正文。

把正文写入工作文件，通过交付工具提交 narrative，然后调用节点结束工具。
