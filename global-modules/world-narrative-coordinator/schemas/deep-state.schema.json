{
  "type": "object", "required": ["status", "currentRunId", "lastTriggerTurn", "lastTriggerWorldTime", "lastCompletedTurn", "lastCompletedWorldTime", "currentReportRevision", "triggerReasons", "failure"],
  "properties": { "status": { "enum": ["idle", "running", "failed"] }, "currentRunId": { "type": ["string", "null"], "description": "Stable identity of the deep operation that owns this state, not the wrapper's or the child's run id." }, "currentChildRunId": { "type": ["string", "null"], "description": "Run id of the module workflow performing the operation, so a restart can reattach to it instead of starting a second child. Optional: operations recorded before this field existed simply have no recorded child." }, "lastTriggerTurn": { "type": ["integer", "null"], "minimum": 0 }, "lastTriggerWorldTime": { "type": ["string", "null"] }, "lastCompletedTurn": { "type": ["integer", "null"], "minimum": 0 }, "lastCompletedWorldTime": { "type": ["string", "null"] }, "currentReportRevision": { "type": "integer", "minimum": 1 }, "triggerReasons": { "type": "array", "items": { "type": "string" } }, "failure": { "type": ["string", "null"] } }, "additionalProperties": false
}
